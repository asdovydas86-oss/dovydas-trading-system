# Copy this file to .env and fill in real values.
# .env is git-ignored and must NEVER be committed — it stays on this Mac only.

# --- TradingView MCP (Chrome DevTools Protocol bridge) ---
# Port TradingView Desktop exposes for CDP. Must match scripts/tradingview-launcher.sh.
TV_DEBUG_PORT=9222

# --- Add any additional API keys / broker tokens / webhook secrets below ---
# EXAMPLE_API_KEY=
# EXAMPLE_API_SECRET=
