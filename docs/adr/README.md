# Architecture Decision Records

An **ADR** records one architectural decision: the context that forced it, the decision itself, the
alternatives that were rejected, and the consequences that follow. Its purpose is to preserve **why**,
because *what* is already visible in the code and *why* is not.

## Relationship to the architecture document

[../ARCHITECTURE_AND_ROADMAP_V1.md](../ARCHITECTURE_AND_ROADMAP_V1.md) §12 holds the decision table
**D1–D11**. That table remains authoritative for those eleven decisions. This directory is the
[docs/README.md](../README.md) "dedicated per-decision ADR directory" moving from *Planned* to *exists*,
and it is used for decisions that need more than a table row — typically because the reasoning is subtle,
the alternatives are individually defensible, or the decision constrains code that has already been
written.

Both forms coexist. Nothing in §12 is migrated retroactively; new significant decisions are written here.

## Conventions

- One decision per file: `ADR-<4-digit>-<kebab-case-title>.md`, numbered in creation order.
- Numbers are never reused, and a superseded ADR is never deleted — it is marked `Superseded by ADR-NNNN`
  and left in place, because historical outputs were produced under it.
- Status is one of: `Proposed` · `Accepted` · `Superseded` · `Rejected`.
- Every ADR states the decision, the **alternatives considered and why each was rejected**, the
  consequences (including the ones the project must live with), and how the decision is enforced.

## Index

| ADR | Title | Status | Decides |
|---|---|---|---|
| [ADR-0001](ADR-0001-canonical-utc-timestamps.md) | Canonical UTC timestamps | Accepted | Which timezones a canonical model may store, and that timestamps are validated but never converted |
| [ADR-0002](ADR-0002-alignment-as-temporal-comparison-policy-layer.md) | Alignment is a temporal-comparison policy layer | Accepted | Alignment moves from `fmis.data` to a dedicated `fmis.alignment` package (review finding R2); code move planned as Milestone I-E |
| [ADR-0003](ADR-0003-availability-time-boundary.md) | Availability-time boundary | Accepted | No macro/released/revised/vintage data until an explicit availability-time model is designed and accepted (review finding R3); corrects the "look-ahead structurally prevented" phrasing |
| [ADR-0004](ADR-0004-rve-v1a-return-and-result-policy.md) | RVE v1a return & result policy | Accepted | Simple returns only, unannualized Bessel volatility, explicit upstream alignment, hybrid raise/undefined result policy, scalar-only scope (Milestone J v1a); refines RVE_DESIGN_V1 |
| [ADR-0005](ADR-0005-ingestion-boundary-strictness.md) | Ingestion boundary: strict decoding, no repair | Accepted | `fmis.ingest` decodes untrusted records into canonical models with no coercion, sorting, or repair; a decoder, not a provider adapter; candles only (observation ingestion gated by ADR-0003) |
| [ADR-0006](ADR-0006-provider-adapter-contract.md) | Provider adapter contract | Accepted | Adapters live in `fmis.providers` with one-way imports; the adapter parses and the boundary validates; `is_closed` derived from an injectable clock; injected transport that never raises on HTTP status; explicit errors that never become an empty series (Milestone P, Binance) |
| [ADR-0007](ADR-0007-application-layer-boundary.md) | Application layer boundary | Accepted | `fmis.pipeline` orchestrates every engine and no engine imports it; orchestration only (no calculation defined there); closed candles unconditionally; warm-up is a result but insufficient data raises; nothing partial and no re-wrapped errors (Milestone Q) |
| [ADR-0008](ADR-0008-decision-support-evidence-boundary.md) | Decision-support evidence boundary | Accepted | `fmis.decision_support` consumes `AnalysisSnapshot` only and nothing below imports it; classification not calculation (one derived value, isolated); an RSI band is never directional; alignment ties stay ties; scenarios restate observations and never forecast; `OverallState` is undirectional (Milestone R) |
| [ADR-0009](ADR-0009-trading-analysis-context-boundary.md) | Trading analysis context, and the trading/investing boundary | Accepted | `fmis.trading_context` states a trading analysis's objective explicitly; swing and day trading are separate objectives; **long-term investing is deliberately not a trading objective** and becomes its own module; shared calculations do not imply shared decision logic; the objective is never inferred from a timeframe; no strategy rules, presets, or pipeline integration yet (Milestone S) |
| [ADR-0010](ADR-0010-volume-foundation.md) | Volume foundation: shared measurement, deferred interpretation | Accepted | `AverageVolume`/`RelativeVolume` in the existing feature engine, sharing one `trailing_mean` kernel; the baseline **excludes** the current candle (warm-up `lookback + 1`); a zero baseline is undefined, never infinity or epsilon; no labels or thresholds — **shared calculation does not mean identical interpretation across markets** (Milestone T) |
| [ADR-0011](ADR-0011-evidence-taxonomy.md) | Evidence taxonomy: a calculated indicator is not automatically evidence | Accepted | `fmis.evidence` defines `EvidenceFamily` (subject areas) and `EvidenceDescriptor` (concept definitions) plus an immutable catalog holding only concepts the system genuinely classifies today — six, in two families, with five families deliberately empty; supporting/conflicting and availability are **not** redefined (already owned by `decision_support`); integration deferred (Milestone U) |
| [ADR-0012](ADR-0012-market-structure-foundation.md) | Market structure foundation: deterministic swing detection | Accepted | `fmis.market_structure` detects swing highs/lows over **closed candles only**, never inspecting a candle outside the confirmation window, so a point once emitted is never revised; strict-left / `>=`-right resolves plateaus to one point while keeping separated equal highs distinct; a package rather than a Feature because `SwingPoint` is not a `FeatureValue`; all structural interpretation (BOS, CHoCH, HH/HL, S/R, liquidity) postponed (Milestone V) |
| [ADR-0013](ADR-0013-swing-relationship-foundation.md) | Swing relationship foundation: comparison without naming | Accepted | Consecutive swings are compared **same-type only** into a numeric `SwingRelation` (HIGHER/LOWER/EQUAL, never bullish/bearish); HH/HL/LH/LL naming is deliberately withheld so `type` and `relation` stay two separate facts; **exact** price comparison because no tick-size abstraction exists; input order is validated, never sorted — with global strictness relaxed to per-type so outside bars still compose (Milestone W) |
| [ADR-0014](ADR-0014-structural-swing-label-foundation.md) | Structural swing labels: naming a fact, not reading it | Accepted | Six `StructuralSwingLabel` members from one authoritative `SwingType` x `SwingRelation` mapping; **full names canonical** (`LH`/`HL` differ by a transposition and mean opposites); `EQUAL_HIGH`/`EQUAL_LOW` first-class and never folded into double-top / support / liquidity; mapping private; input order preserved; outside-bar equal indices accepted; BOS, CHoCH and trend remain later milestones (Milestone X) |
| [ADR-0015](ADR-0015-structural-sequence-state-foundation.md) | Structural sequence state: two sides read together | Accepted | The latest HIGH-side and latest LOW-side labels combine into five states over a **complete nine-cell matrix** (outward/inward/static per side), plus one `INSUFFICIENT_STRUCTURE` — no catch-all member, `ADVANCING_*`/`BALANCED` rejected as interpretive; both source `StructuralSwing` objects retained because the grouping is lossy; outside bars resolve **atomically** to one final state; the aggregate is **expected to evolve and prefix stability is explicitly not claimed for it**, only for the swings beneath; one shared ordering rule, state history postponed (Milestone Y) |
| [ADR-0016](ADR-0016-structural-sequence-state-history-foundation.md) | Structural sequence state history: recording a sequence, not reading it | Accepted | One `StructuralSequenceStateSnapshot` per candle that changed structure, outside-bar groups applied **atomically**; state embedded whole and `index`/`timestamp` exposed as **projections, not fields**; `INSUFFICIENT_STRUCTURE` snapshots recorded rather than suppressed; prefix-stable under **candle-series and complete-group extension only**, with the arbitrary-inside-group cut explicitly outside the guarantee; no transition type and no "changed" flag; both single-state and history APIs kept under a tested equivalence contract (Milestones Z0-Z1) |
| [ADR-0017](ADR-0017-structural-trend-foundation.md) | Structural trend: summarising a sequence, and naming the policy that does it | Accepted | A **structural trend** is a *sustained same-direction structural shift* — at least `MINIMUM_DIRECTIONAL_SHIFTS` (2) snapshots of the same directional state with no opposing one between; only `SHIFTED_HIGHER`/`SHIFTED_LOWER` are directional evidence and the other four states are **transparent**, so a trend persists through them and is invalidated by exactly one opposing shift; `NEUTRAL` (evidence conflicts) and `INDETERMINATE` (evidence absent) are kept distinct so ambiguity is reported rather than resolved; the threshold is a **stated policy, not a measurement**, and a module constant rather than a parameter; prefix-stable under candle-series and complete-group extension with the inside-group cut explicitly excluded; a **sibling package** `fmis.structural_trend` so `market_structure` keeps its no-interpretation guard; still no evidence descriptor, and trend is never an input to a break (Milestone AA) |
| [ADR-0018](ADR-0018-series-identity-and-context-contract.md) | Series identity: extracting what `CandleSeries` already knew | Accepted | One analytical series is `symbol` + `timeframe` — the pair `CandleSeries` has always validated per candle — extracted as `SeriesIdentity` in `fmis.data` with `CandleSeries.identity` as a **projection, not a field**; the gap was propagation, not definition, since `detect_swings` reads neither. Context lives **once** in an immutable generic `ContextualSeries` envelope, never per element and never inside the values the maths reads, so adding it provably changes no analytical result; equality is exact with **no normalization** (inherited from ADR-0011/ADR-0009), so the contract deliberately **over-rejects**; `require_same_identity` is the single choke point and spans both a `CandleSeries` and a `ContextualSeries` so Level-Crossing needs one check and no dependency on trend; context-free primitives stay public and unchanged, classified as such (Milestone AB) |
| [ADR-0019](ADR-0019-level-crossing-foundation-v1.md) | Level-crossing foundation: a crossing is a fact, a break is a reading | Accepted | `fmis.level_crossing`, the **first layer reading both candles and derived structure**, closing review §15's gap that after `detect_swings` no layer read a candle; one canonical non-configurable policy where exact equality is a `TOUCH` and only strict `>`/`<` is a breach, with wick and close as two *facts on the event* rather than a setting; an orthogonal `CrossingMechanism` separating price *reaching* a level from *arriving beyond* it and from a series that *starts* beyond it, with `open` never consulted; **no intrabar path and no "order unknown" flag** — a flag that never varies carries no information; **no lifecycle at all** (no activation, no first-cross-only), because both are BOS policies a consumer applies by filtering fields the event already carries; ordering total and self-validated from explicit rank mappings, duplicate levels rejected rather than deduplicated; prefix stability **exact with no exceptions** since nothing reads forward; identity via one `require_same_identity(series, levels)` call and **no trend dependency**, keeping review §15's ordering (BOS on levels → CHoCH over BOS → trend a summary of both) (Milestone AC) |
| [ADR-0020](ADR-0020-break-of-structure-foundation-v1.md) | Break of structure: the first close beyond a level that was already knowable | Accepted | `fmis.structure_break`, the first layer built entirely on **derived facts** — it does not import `fmis.data` at all, so a candle is not a name it can reach; a break is the **first CLOSE_BREACH** of the **most recent eligible** level on its side, with `TOUCH` and `WICK_BREACH` excluded (a wick that closes back inside is a rejection, and a wick rule cannot be non-repainting) and the policy **not configurable**; the **label decides nothing** — all six labels can produce a reference level, and it is carried for a consumer to weigh; **structure breaks once** per level and is never invalidated; `GAPPED_BEYOND` qualifies, `ALREADY_BEYOND` does not. The audit found one **missing primitive** and documents it rather than inventing behaviour: the confirmation delay is on no derived fact, yet pivot-bar eligibility is **prefix-unstable** (30 violating prefixes across 40 fixtures), so `confirmation_bars` is a **required argument with no default** and carrying it on `LevelOrigin` is deferred; no breach can occur inside a level's own confirmation window (11,608 in-window crossings, all TOUCH), so the rule discards no reachable break. CHoCH is then definable over the break sequence alone (Milestone AD) |
| [ADR-0021](ADR-0021-change-of-character-foundation-v1.md) | Change of character: the first break opposing the last determinate one | Accepted | `fmis.change_of_character`, the layer that completes the deterministic structural chain — it reads **no candle, no level and no crossing**, taking the single type name `LevelSide` from `fmis.level_crossing` and nothing else. A change is a break whose side differs from the side broken at the **most recent strictly earlier** break-bearing bar, when that bar broke **exactly one** side. The audit found **no missing primitive** — the first milestone in the chain to add none and request none — and instead found that **ADR-0020 §7's `zip` adjacency sketch is wrong**: it infers a change of character from the order of two breaks sharing a bar, which ADR-0019 §2.6 and ADR-0020 §3.5 both refuse to read as temporal. That sketch is **superseded** and the disagreement is pinned by a test; no production source was modified. A **two-sided break bar leaves character indeterminate** (E1, the principal limitation), suppressing without persisting. At most **one change per bar**, so ordering is by index alone and **no side ordering exists in the package**. Trend remains a summary of BOS and CHoCH, defining neither — review §15 satisfied end to end |
| [ADR-0022](ADR-0022-structural-fact-sheet-composition-root.md) | Structural Fact Sheet: a second composition root, and containing the confirmation delay | Accepted | The first end-to-end consumer of the structural chain, closing the split Report 0003 measured — `fmis` was **two dependency islands with zero import edges between them**, the structural half being 51.2 % of the codebase with no provider path and no consumer. A second composition root **inside `fmis.pipeline`** rather than a new package, because ADR-0007 §1 already defines that boundary; **zero arithmetic**, one stricter than ADR-0007 §2 allows its sibling, by reusing `_window_of` and selecting nearest levels by comparison instead of distance. The **confirmation delay has exactly one source**: a frozen `DetectionSettings.right_bars` read once and handed to both `detect_swings` and `derive_structure_breaks`, making a mismatch unrepresentable through this root — **containment, not a fix**, since ADR-0020 D1's real fix changes five shipped models across three packages and stays its own milestone; a `right_bars` parameter on `structural_levels` was rejected as a *fake fix* that merely relocates the hazard while making a wrong value look like provenance. A sheet is a **pure function of its candles** with no clock beneath the CLI, so freshness needs an injected reference. Levels are reported as **nearest above / nearest below**, never support and resistance, which ADR-0019 §I reserves — the live BTCUSDT run shows why, the nearest level *below* the close being an **UPPER** one. Fact-only vocabulary is enforced **through to the terminal**, with `signal_line` and the closing disclaimer the two justified exemptions (Milestone AF) |
| [ADR-0023](ADR-0023-multi-timeframe-composition.md) | Multi-timeframe composition: three facts side by side, nothing derived from their combination | Accepted | `fmis.pipeline.multi_timeframe`, a third composition root that calls `structural_facts_for_symbol` once per timeframe and reaches no engine of its own. **No cross-timeframe synthesis** is the load-bearing decision: no agreement, alignment, conflict, consensus or score — a `TrendAgreement` field was considered and rejected because classifying the *combination* is the Market Regime Engine's job, and emitting it here would put the first interpretation into the application layer. Roles (`CONTEXT`/`SETUP`/`EXECUTION`) are **stated, never inferred from the interval**, following ADR-0009; role order comes from an explicit rank map, not enum definition order. Views are **not aligned in time** and each keeps its own `as_of` — the weekly view's newest bar measured 13 days old live — so `newest_as_of` is named to prevent it reading as a shared instant, and `fmis.alignment` is deliberately unused. **Nothing partial**: any short timeframe raises. Zero arithmetic and no clock, both AST-asserted; one `DetectionSettings` reaches every view so ADR-0020 D1's containment extends unchanged, still contained and still not fixed. `swing_features()` adds EMA(200) without touching `default_features()`, whose 60-candle test fixture EMA(200) would break. Commands move to a declared registry (Milestone AG) |
| [ADR-0024](ADR-0024-confirmation-delay-provenance.md) | Confirmation-delay provenance: the delay travels with the origin that earned it | Accepted | Closes **ADR-0020 D1**, the hazard two milestones could only contain: `derive_structure_breaks` required a `confirmation_bars` argument that lived on none of its inputs, so a value disagreeing with detection silently changed which level was the reference at every bar — **36.1 % of 300 seeded series produced materially different breaks and none raised an error**. The window is now stamped at detection onto every `SwingPoint`, copied onto every `LevelOrigin`, and read from there; **the argument is removed** from `derive_structure_breaks` and `contextual_structure_breaks`, so the mismatch is unrepresentable rather than validated — a `structural_levels` parameter was rejected again as the *fake fix* that relocates the hazard while making a wrong value look like provenance, and no optional-override compatibility path was kept because it would preserve two sources of truth. The property is `knowable_from`, a fact about **detection**, deliberately not `eligible_from`, which is a break-of-structure decision that stays in that package — the first attempt, `confirmed_at`, was rejected by `fmis.market_structure`'s own vocabulary guard and the **name changed rather than the guard**. `StructureBreak.eligible_from` becomes a projection, removing a second source of truth and two validations that are now arithmetic guarantees; a window of **at least 1** is required, making a break at bar 0 unrepresentable; one level set must **agree on one window**, because strictly-increasing eligibility is what the binary-search reference lookup and the single-test eligibility rule both rest on. Both composition roots lose their duplicated delay configuration and **`ADR-0020 D1` leaves the printed limitations**. No package's `__all__` changed; behaviour is proved unchanged against a reimplementation of the pre-AH algorithm (Milestone AH) |
| [ADR-0025](ADR-0025-market-regime-engine-v1.md) | Market Regime Engine v1: the environment in three dimensions, never a direction | Accepted | `fmis.market_regime`, an engine at **L5** built to `ARCH` §9 — an explicit, testable classification carrying **evidence and uncertainty** rather than a bare word, replacing the regime call buried in the v3 prompt's STEP 1. The design is the v2 post-mortem read as a specification: evidence votes by **family** and the four families partition across the three dimensions, so the double-counted trend gate that gave LONG two free confirmations cannot recur; a threshold band is **one** number whose edges are `1 + band` and `1 / (1 + band)`, so the asymmetric gate cannot be written through the API or the CLI; and the engine **never learns direction** — `SUSTAINED_HIGHER` and `SUSTAINED_LOWER` are one tuple with no branch between them, asserted by comparing whole regimes. This **deliberately diverges from v3**, which classifies BULLISH/BEARISH/RANGE. Three dimensions (structure · volatility · participation) stay separate with **no overall state and no score**, because a composite label is the bare word §9 argues against and a number would imply a calibration never performed; `INSUFFICIENT` (absent) and `INDETERMINATE` (disagreeing) are distinct, structure refuses to classify from one family, `UNAVAILABLE` is never conflicting, and a fourth `CONTEXT` status follows `decision_support`'s `NOT_DIRECTIONAL` precedent. The boundary is a **narrow `RegimeInput`** — the engine imports one repository name and cannot see the application layer, which the root adapts into with **zero arithmetic**. Volatility is a **ratio of two ATRs** so the threshold is asset-agnostic; `fmis.features.VolatilityRegime` was not reused, being a *level* vocabulary for an unbuilt feature. A **new command** rather than a changed one, leaving `facts` and `mtf` byte-identical (Milestone AI) |
| [ADR-0026](ADR-0026-decision-context-boundary.md) | Decision context: one question about the information, never about the market | Accepted | `fmis.decision_context` answers *does this analysis contain enough trustworthy information to continue toward a trading setup* — and nothing else. Built because a **12-candle page and a 260-candle page rendered nearly identically**: a section's status says whether it produced output, not whether the output can be trusted, and nothing aggregated the difference. It **does not contradict ADR-0025's refusal of a composite label** — that refusal was about collapsing *market state*, while this judges the *analysis*, which `SPEC` §6 requires as a first-class output. Five requirements, and **every one delegates its rule to the layer that already declared it**, so `ContextPolicy` is the only policy object in the repository carrying **no numbers** and a test asserts no numeric literal beyond 0 and 1 exists in the evaluator. Severity is fixed in code with a single `strict` flag rather than per-requirement overrides, because a demotable severity set lets a caller switch off the one check about to stop them. **Conflicts never move the verdict** — sufficiency is about availability, and penalising disagreement rewards one-sided pages. Three states, no score. The engine imports **nothing** from `fmis`, which matters because the `Workspace` model is presentation-shaped and consuming it would parse formatted strings back into data; `may_continue` derives from the state rather than the checks after an earlier version reported `INSUFFICIENT` and may-continue simultaneously under strictness (Milestone AL) |
| [ADR-0027](ADR-0027-memory-and-decision-archive-persistence-schema.md) | Memory & Decision Archive persistence schema (closes D-01) | Accepted | `fmis.archive` persists a completed `Workspace`/`DailyRun` as an explicit, hand-codec'd, UTF-8 JSON envelope — never `pickle`, never reflection. **Snapshot reproduction only**: `decode(encode(x)) == x`, exactly; no re-computation and no historical replay, stated as a limitation rather than implied. `content_digest` is a SHA-256 over exactly `{record_type, schema_version, analysis_as_of, subject, payload}`, deliberately excluding `record_id` (derived *from* the digest — including it would be circular) and `archived_at` (a filing timestamp that must not affect whether two archive calls of the same analysis are duplicates). `record_id` is content-derived (`{type}-{subject}-{UTC timestamp}-{digest prefix}`), making "same identity, different content" reduce to a 16-hex-character digest-prefix collision (1-in-2^64) rather than a state the design has to arbitrate — widened pre-push from an 8-hex-character (32-bit) draft that was rejected before release. Every write is `mkstemp`-same-dir → fsync → `os.replace`, so a reader never observes a partial record; the manifest (`manifest.jsonl`, metadata-only, so `archive list` never opens a payload) publishes only after the record file exists, via the identical atomic sequence. No migration path exists yet: an unsupported schema version — envelope or payload — is rejected cleanly rather than guessed at (Milestone AO) |
| [ADR-0028](ADR-0028-directional-interpretation-boundary.md) | Directional interpretation boundary: where LONG/SHORT may exist | Accepted | `fmis.swing_setup` is the one package permitted to hold directional trade vocabulary (`LONG`/`SHORT`, entry/stop/target), at the same top-level tier as `fmis.workspace`, plus its one necessary consumer at the outermost edge, `pipeline/cli.py` — the same shape as the `fmis.archive` exception ADR-0027 established. It imports only public surfaces of `fmis.pipeline`, `fmis.decision_support`, `fmis.decision_context`, `fmis.market_regime` and the read-only structural types it needs, and never `fmis.workspace`/`fmis.daily`/`fmis.archive`, duplicating ~20 lines of pure adapter plumbing rather than creating a peer-to-peer top-layer dependency. A directional candidate may never be inferred from one evidence family, and never when `fmis.decision_context` reports `INSUFFICIENT`; `WAIT` is a first-class, successful result. A repository-wide guard test scans every other package's source for the same forbidden-word list the existing page/package-scoped guards use, additive to and not replacing them (Milestone AR) |
| [ADR-0029](ADR-0029-money-and-numeric-semantics.md) | Money and numeric semantics: where `Decimal` is required and where `float` stays | Accepted | **Ratifies an implemented boundary; changes no production code.** Slice 4's audit found the money contract fully built and shipped since Milestone BI — `fmis.money`'s exact, asset-tagged `Money`/`Quantity`, sixty-odd dependent modules, canonical text persisted by the store — and **no ADR about it anywhere in 0001–0028**, the rule living only in `AP` §5.3 and module docstrings. Every asserted monetary and quantity value is `Decimal`; **there is no bare number**, a monetary field always carries its asset, and cross-asset arithmetic **raises rather than converting** because a conversion needs a dated rate with its own provenance. The market half **keeps `float` deliberately** — a candle close is a measurement, not an assertion, is never summed into a balance, and ADR-0013 §4 is untouched; converting it would impose exactness a measurement does not have. `exact_from_market_price` is the **one named crossing**, via `repr` so `0.1` becomes `Decimal("0.1")` and not its fifty-five-digit expansion. Canonical text is **part of identity**: `Decimal("0.10")` and `Decimal("0.1")` are equal as values and produce different digests, and since `event_id` derives from the digest two spellings would be two events — so every operation runs in a pinned `Context(prec=34)` rather than the mutable thread-local one. `NaN`/`Infinity` are rejected at construction. **Rates, fractions and ceilings follow money, not measurement**, which Slice 4 forced: `0.02` as a binary float is `0.0200000000000000004163…`, so a ceiling compared against it decides a capital-preservation limit by representation error; a `float` fraction is a `TypeError` and a JSON number in the owner's config is refused by name. Rejected: converting the market half (cost without correctness gain), integer minor units (per-asset scale unknowable, wrong scale silent), and `float` for display-only money (the display path is where a wrong number is acted on) (Slice 4) |
| [ADR-0030](ADR-0030-risk-policy-declaration-boundary.md) | Risk policy declaration boundary: where the specification's 2 % lives, and the only producer of a `RiskBudget` | Accepted | `fmis.risk_policy` is the owner's declaration boundary, created because the audit found ~8,300 lines of correct, tested, guarded risk capability that **had no first link**: `RiskBudget(` and `RiskLimit(` appeared **nowhere in `src/`**, only in four test files, so `per_trade_ceiling` was never consulted, `SizingPolicy.fraction_for` never resolved, `ApprovalEngine` never ran against real limits, and the dashboard showed no figure and could not say why. `SPEC` §8.1's **2 % lived in no code at all** and could not live in `fmis.risk` or `fmis.position_sizing`'s computing modules, both AST-guarded to hold no literal beyond `0` and `1` — a guard this ADR **does not weaken**, siting the number instead at the boundary where the owner's words become a domain object. The ceiling is **structural, not validated**: a declaration above it cannot be constructed, so there is no object to render and no branch to forget; `<= 2 %` in and `> 2 %` out, exact on both sides. It is **never a default** — `budget_from` leaves `default_below_ceiling` `Absent` because the specification states a maximum and no default, and the owner's fraction travels on `SizingPolicy.risk_fraction` (`fraction_for` source 1) instead, so an undeclared fraction resolves to **no fraction** rather than to the ceiling; expressing it as `default_below_ceiling` was considered and rejected because `RiskLimit` requires a default strictly below the ceiling, making an exact-2 % declaration unrepresentable. Capital is **declared and `ASSERTED`**, dated, read from `~/.fmits/risk_policy.json` — deliberately **not** `PortfolioState.equity`, which is `MEASURED` from fills the owner does not yet have and which inventing would be fabricating their holdings. **Declared, not stored**: persisting on read would make rendering a page a write, and `RiskRepository` stays the home for a versioned lineage the day one is recorded deliberately. The file's keys are a **closed set**, so an API key or secret is refused **by name** — a loader that skipped unknown keys would make a credential invisible rather than impossible. **Risk observes the decision and never participates in it**: no import of `swing_setup`, `setup_evidence` or `decision_support`, a vocabulary guard banning `confidence`/`probability`/`score`/`evidence`/`independence` as identifiers, and a whole-record comparison proving every decision field, group, summary and ordering identical with and without a policy. **No geometry invented** — entry is the engine's reference price carrying its *not an order price* caveat, no stop from ATR, no assumed target. Portfolio impact is **always a stated absence** (`remaining_open_risk=None`, *not* `Absent`), because unknown open risk is not zero and a plan inside the per-trade ceiling is not one the book has room for. Scope explicit: linear spot-like, one trade in isolation, before costs, planned risk not worst case, **no leverage** — leverage magnifies a loss and never brings one inside a ceiling it is outside of (Slice 4) |
| [ADR-0031](ADR-0031-feature-series-contract.md) | The additive feature-series contract: `compute_series()`, alignment, warm-up and provenance | Accepted | `Feature.compute()` returns **the latest value only**, so slope, rate of change, acceleration, crossover recency, divergence and volatility compression are **not derivable in principle** — review item **R5, open since 2026-07-24**, re-measured by report 0047 §8 seam 1 and approved **as additive** by the 0047 review disposition §C and D4. A second Protocol `SeriesFeature` is added **beside** `Feature`, which is untouched; `BaseFeature` declares no abstract `compute_series`, so a feature whose output is not a history stays a valid `Feature` and `supports_series()` has two honest answers. Report 0047 observed that `FeatureValue` already permits `Sequence` so *no contract change is needed* — **true and not sufficient**, and the reason this ADR exists: a bare list answers none of alignment, warm-up boundary, undefined-versus-absent, or series identity, and every reconstruction of those is a place to be off by one. The result is an explicit `FeatureSeries` — `name` · `category` · `SeriesIdentity` · `as_of` · `closed_candles` · `warmup_candles` · `points` · `metadata` — whose points carry the **closed-candle index** (the same index space `SwingPoint`, `LevelOrigin` and `LevelCrossingEvent` already use) and their own candle's timestamp. Identity is stored **once for the whole series**, the `ContextualSeries` convention (ADR-0018). `as_of` is the last **closed candle**, deliberately not the last point: they differ exactly while warming up, and collapsing them makes a warming-up series look stale. **Warm-up is never backfilled** — a position before it carries no point at all, not a `None`, a zero or the seed — and `first_index`/`latest` are projections over `points`, never stored (ADR-0016 §4). A point carries a `value` **or** an `undefined_reason`, never both and never neither, so *not warmed up* / *undefined here* / *computed* stay three distinguishable facts — `RelativeVolume` over an all-zero baseline is the live case. Values may be **structured**: MACD emits its three-component mapping per point, which is the test that the protocol is not scalar-only. **One mathematical implementation**: each converted indicator's arithmetic lives in exactly one pure `*_math` helper producing the whole series in one pass, and `compute()` takes its last element — so `compute(context).value` **equals** the final point exactly, bit for bit, asserted per feature rather than approximated. Prefix stability and no-lookahead are contract and are tested. **No engine-level `compute_series`**, deliberately: `FeatureEngine` threads each feature's *latest* result into the next feature's context, and threading latest values into a historical computation injects lookahead produced by the orchestration; no feature declares a dependency today, so nothing is blocked. Six features converted — EMA, ATR, RSI, MACD, `RelativeVolume`, `AverageVolume` — and `AverageVolume` stays **DORMANT**, not promoted into `default_features()`. Measured O(n) for the recursive indicators and O(n · lookback) for the volume baseline, lookback being a parameter and not a function of n. Rejected: a list in `FeatureResult.value`; reusing `ObservationSeries` (no candle index, no structured value, no warm-up field); widening `ContextualSeries` (deliberately two fields, and three structural engines depend on it); parallel tuples; `None`-padding the warm-up; changing `compute()` (breaks every caller **and** the policy digest); an abstract `compute_series` on `BaseFeature` (TA Slice 5A) |
| [ADR-0032](ADR-0032-market-technical-context-carriage.md) | Market technical context: a sibling of strategy input, never a wider strategy input | Accepted | `build_setup_inputs` receives three complete `StructuralFactSheet`s and emits **twenty-two scalars and two level tuples**; report 0047 §7 traced every discarded fact to its line and `CAPABILITY_REGISTRY.md` §2.2 re-verified each against live code — the context-role `levels`, every `crossing`, every `ChangeOfCharacter`, the `nearest_levels` pair, the setup-role `breaks`, the setup- and execution-role `MarketRegime`s and all three `FeatureSet`s, `PRODUCT_UNREACHABLE` and the cheapest class of gap in the repository. **The obvious repair is the one refused**: adding the fields to `SetupInputs` would make the strategy's own input surface the warehouse for all technical analysis, so *what may the policy see* stops being answerable by reading one dataclass, and it would invite a later milestone to gate on a recovered fact **because it was in scope** — the correlated-vote inflation §3 of the registry measures and forbids. Report 0047 identified the **seam** and did not decide the **ownership**; this does. A `MarketTechnicalContext` is assembled **after** the assessment from objects the composition already built, carried **beside** it — generalising the precedent `SetupReadings` set in Slice 1, whose docstring already says *it is not a policy input and the policy never reads it*. `SetupInputs` gains **no field** and `evaluate_setup` is untouched byte for byte. Owned by **`fmis.pipeline`**, which already owns the sheet (ADR-0022, ADR-0023); no new top-level package, and the composition-root discipline is inherited — a test asserts the module contains **no arithmetic operator at all**. **Roles are never blended**: three per-role views in the sheet's canonical order, no agreement flag and no alignment count, `MultiTimeframeFactSheet`'s load-bearing refusal inherited verbatim. Canonical objects are carried **by reference** — `PriceLevel`, `LevelCrossingEvent`, `StructureBreak`, `ChangeOfCharacter`, `MarketRegime`, `FeatureSet`, all frozen, none copied — because *lossless* means what a later zone or interaction engine actually needs: an integer count cannot answer *which level, when, how far, how it arrived*. **Bounded summaries are selected here, never in a renderer**: the latest crossing and the latest close breach are the last elements of a sequence the deterministic layer already ordered and classified, chosen beside the full history they come from, and the full run stays reachable while never being rendered. **Carriage is not interpretation** — a `CLOSE_BREACH` is not a breakout, a `ChangeOfCharacter` is not a reversal, the nearest level above is not resistance (ADR-0019 §I, disposition §E), an indicator value is not a lean — and there is no field an opportunity, score, probability or ranking could live in. It reaches `SetupRunResult.technical` → `SymbolDecision.technical` → the dashboard as an **additive, defaulted** field, typed `Any` on the workspace for the reason `developing`/`blocker`/`plan` are, and reaches **`fmis.scan_memory` not at all** — an EMA value that moved every refresh would turn *what changed* into noise. Rejected: widening `SetupInputs`; a new top-level `fmis.technical_analysis`; carrying the raw sheet (hands a renderer the candles); reducing crossings to a count; stringifying at the seam (seam 5 of report 0047 is this repository having already done it once); extending `SetupReadings` (would put `fmis.level_crossing` and `fmis.features` vocabularies inside the strategy package) (TA Slice 5A) |
| [ADR-0033](ADR-0033-price-zone-semantics-and-the-tolerance-boundary.md) | Price zone semantics and the tolerance boundary | **Accepted** 2026-09-18; implemented by TA Slice 5B ([report 0051](../../reports/0051_2026-09-18_TECHNICAL_ANALYSIS_SLICE_5B.md)) as `fmis.price_zones` with **`k = 0.50` declared** by the owner and **one policy shared by 1W/1D/4H**; `ZoneInteraction`, `ZoneReading` and the role vocabulary shipped **not at all**, deliberately | Closes report 0047 **D1** and **D2**. **D1 reads as one decision and is three** — the distance scale (0047 §15.3's whole argument), the **grouping geometry** (§15.2 says *"clustered"* and stops) and the temporal reference for a volatility-derived scale — and the geometry, the one 0047 never discusses, decides everything. Measured over **55,728 policy cells**, 36 symbols × 3 roles, offline: **single-linkage clustering chains** (247 of 339 levels into one zone on BTC 1D; zones up to **94× the policy's own tolerance**), and a **diameter-bounded greedy is not reflection-symmetric** (2 % of cells) while rewriting history on **120,441** prefix events. Construction is therefore **anchored and online, never clustering**: levels are processed in the order the market made them knowable, each joining the band it falls inside or opening one anchored at its own price, and **zones are never merged, split, resized or deleted** — prefix stability becomes a property of the construction rather than a claim checked afterwards, and it measures **0** illegal events. The band is `anchor.price ± w/2` with `w = k × atr_14` read at **the anchor's own establishment bar** and **frozen**: width from current ATR produced **983,916** illegal prefix events (every historical zone redrawing its own boundaries every bar) and letting a joining member widen the band produced **34,192** (a level outside the zone yesterday is inside it today with no new information about that level). No ATR at the anchor's bar → **no zone**, never a default width. Ties go to the **oldest containing band**, not the nearest centre — nearest-centre compares two nearly equal floats and flipped on **35 %** of series under a rescale, and this is **the preregistration's own rule being refuted by its own battery**. **One bar is one instant**: a candle can be both a swing high and a swing low, so ordering two same-bar levels must break the tie on side or price, both of which invert under reflection. Membership is **by price alone** — an `UPPER` and a `LOWER` level may share a zone, membership is unique, and **bands overlap** (70 % of adjacent pairs at `k = 0.50`), so a price may sit inside several zones and consumers must handle that. **`k` is declared, not measured, and the ADR says so in those words**: every descriptive metric is **monotone** in `k`, so there is no plateau, no optimum and no knife-edge — `k` is a **resolution control**, and a study that deliberately excludes outcome data cannot derive one. The evidence fixes only the region `k ∈ [0.10, 1.00]` (`k = 0` leaves 96 % singletons; `k ≥ 2.00` merges 99.4 % of levels into 44-member zones; long/short symmetry is lost above 1.00). Rejected with reasons: **percentage-of-price** because it is **not long/short symmetric** (6 % of cells) — zero is an absolute floor and there is no corresponding ceiling, a stronger and *measured* objection than 0047's *"invented threshold"*; **absolute/tick** because it is **not expressible** (`fmis.data.Candle` carries no instrument metadata, and `CrossingKind`'s docstring already records that tolerance belongs at ingestion behind a tick-size model that does not exist); **structural spacing** at 696,588 illegal events because its yardstick did not exist when the zone did — and its causal repair was given **the same symmetry fixes the winning geometry received** and re-run before being rejected, on numerical fragility rather than on whose bug was found first. **Existing exact semantics are untouched**: `PriceLevel` equality, `classify_comparison` and `CrossingKind` stay exact and **ADR-0013 §4 is not weakened**; the tolerance is scoped to zone membership alone, so `PriceLevel(A) != PriceLevel(B)` and `Zone.members == {A, B}` are both true by design. **D2**: role comes from **interaction history, never position**, with `position` a separate field — quantified rather than asserted, **22 %** of zones above the close on 1W have no interaction history at all (max 75 %) and **39 % (1W) / 50 % (1D)** have had price close on **both** sides since establishment, so the position rule is not merely sometimes wrong but **not well-defined over time**. Vocabulary fixed (`UNTESTED` · `HELD_FROM_ABOVE` · `HELD_FROM_BELOW` · `BROKEN_UPWARD` · `BROKEN_DOWNWARD` · `ROLE_FLIPPED` · `INDETERMINATE`); *"Support zone"* permitted later only as a label over a derived role and **never** for `UNTESTED`. `ZoneInteraction`/`ZoneReading` are **not** in V1 — each needs a parameter **R3**/**R4** did not answer, and **a `CLOSE_BREACH` is not a breakout**. **No strategy effect**: no zone reaches evidence voting, the 1W gate, Scan Memory or risk, and **independence remains `NOT ESTABLISHED`** until **R15** (report 0050) |
| [ADR-0034](ADR-0034-zone-interaction-evidence-boundary.md) | The zone-interaction evidence boundary | **Proposed** 2026-09-20 | Answers **R3** and **R4**, the two questions [ADR-0033](ADR-0033-price-zone-semantics-and-the-tolerance-boundary.md) named as the reason `ZoneInteraction` and `ZoneReading` are not in V1. Evidence: [report 0052](../../reports/0052_2026-09-20_PRICE_ZONE_INTERACTION_SEMANTICS_RESEARCH_GATE.md), preregistered at `296831a` before its harness existed. **Rewrites nothing in ADR-0033.** **D1 — a persistence state over a band is computable and is *not* a zone fact.** Two consecutive closes beyond a frozen band versus one moves ten-bar persistence from **0.41 to 0.65** (+22 to +25 points, 1D and 4H, primary and holdout); a third adds +26 to +29 more, so **there is no knee and `N` is a resolution control**, as `k` was. **But a placebo band — identical width, identical establishment bar, displaced so that no confirmed structural level anchored it — separates by +0.243 against the real +0.246**, an excess of ±0.004 that flips sign across cells. The state is real; **the attribution to the zone is not**. FMITS may compute it and **may not call it acceptance**. **No `N` is declared**, because nothing needs one. **D2 — there is no `RETEST` and no window to choose.** The return hazard to a real zone is indistinguishable from a clean placebo at every elapsed range on every role (**ratio 0.96–1.02**, several bins significantly *below* 1), under both return definitions and all three excursion preconditions. `RETURN_TO_ZONE_AFTER_OUTSIDE_STATE` is the strongest honest name. **The trap this closes:** the raw hazard decays seven-fold over 40 bars, steepest in the first three, and alone reads as *"retests cluster within 2–3 bars"*; **the placebo decays identically** — first-passage geometry, not market memory. Any future retest window must clear a null before it is discussed. **D3 — six of the seven approved role labels are underivable.** Only `UNTESTED` is available, and it means nothing has happened: *held* is the attribution D2 refused and *broken* the one D1 refused. **The vocabulary is not forced into a state machine because this ADR's predecessor names it.** **D4 — support/resistance labels stay unavailable**, and deriving them from `position` stays forbidden. **D5** fixes what a future interaction slice must clear: an observable event with an explicit knowledge time, **a placebo or equivalent null** (both headline effects survive naive measurement and neither survives a control), inference clustered on the **symbol** never the event (22.9 events per zone), and a name matching what was measured. **Limitations stated so a reader can judge what reopens it:** 1W `UNDERPOWERED` in both universes; **no volume** in the capture so volume-confirmed acceptance is *untested, not refuted*; gapped transitions unmeasurable in continuous crypto; survivorship; conditional on `k = 0.50`; the observed direction asymmetry is a sample property and **not** a licence for direction-dependent semantics. Unchanged: every ADR-0033 zone semantic, `PriceLevel`/`classify_comparison`/`CrossingKind` exactness, independence still `NOT ESTABLISHED` pending **R15**, and no zone fact reaching strategy, risk, Scan Memory or the 1W gate. **A `CLOSE_BREACH` is still not a breakout** |
