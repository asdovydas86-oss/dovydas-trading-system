# Operational Reports

Index of the project's operational reports: audits, design records, implementation records,
reviews, and any other point-in-time analysis produced while working on this repository.

## Purpose

This directory is the single home for **operational reports** — dated, numbered, immutable
records of work performed on the repository.

It is deliberately separate from `docs/`:

| Directory | Holds | Nature |
|---|---|---|
| `docs/` | ADRs, design documents, architecture, review records for milestones | The project's **standing** documentation — maintained and updated over time |
| `reports/` | Audits, analyses, and operational reports | **Point-in-time** records — written once, kept as-is |

A report describes the repository *as it was* at a given commit. It is not revised when the code
moves on; a later report supersedes it instead.

## Filename convention

```
NNNN_YYYY-MM-DD_DESCRIPTIVE_TITLE.md
```

- `NNNN` — zero-padded, four-digit report number
- `YYYY-MM-DD` — the date the report was produced
- `DESCRIPTIVE_TITLE` — uppercase snake case

Examples:

```
0001_2026-07-31_REPOSITORY_AUDIT.md
0002_2026-08-01_LEVEL_ORIGIN_DESIGN.md
0003_2026-08-01_LEVEL_ORIGIN_IMPLEMENTATION.md
0004_2026-08-01_LEVEL_ORIGIN_REVIEW.md
```

## Numbering rules

- Numbering is **global and sequential across all report types** — design, implementation, review
  and audit reports draw from one shared counter.
- A number is **never reused and never overwritten**, including for reports that are later
  archived, superseded, or withdrawn.
- Every new report **must** be added to the index table below in the same change that creates it.
- Reports are **never deleted** unless the user explicitly authorizes deletion.
- Do **not** create generic `REPORT.md` files in the repository root.

**Next available report number: `0053`**

## Metadata header

Each report should open with a short metadata table:

| Field | Value |
|---|---|
| **Report number** | 0001 |
| **Title** | Repository Audit |
| **Date** | 2026-07-31 |
| **Report type** | Audit |
| **Model** | Claude Opus 5 |
| **Repository branch** | `main` |
| **Audited commit** | `d132cea` |
| **Status** | Final |

`Status` is one of: `Draft`, `Final`, `Superseded`, `Archived`.

## Index

| # | Date | Type | Title | Status | Branch / Commit | File |
|---|---|---|---|---|---|---|
| 0014 | 2026-08-12 | Implementation | Trade Domain Foundation (Milestone BH) — Implementation Record | Final | `main` / uncommitted (base `7ced9e2`) | [0014_2026-08-12_TRADE_DOMAIN_FOUNDATION_IMPLEMENTATION.md](0014_2026-08-12_TRADE_DOMAIN_FOUNDATION_IMPLEMENTATION.md) |
| 0015 | 2026-08-12 | Implementation | Trade Repository & Journal Engine (Milestone BI) — Implementation Record | Final | `main` / uncommitted (base `c96c3e4`) | [0015_2026-08-12_TRADE_REPOSITORY_AND_JOURNAL_ENGINE_IMPLEMENTATION.md](0015_2026-08-12_TRADE_REPOSITORY_AND_JOURNAL_ENGINE_IMPLEMENTATION.md) |
| 0016 | 2026-08-12 | Implementation | Daily Trading Workspace MVP (Milestone BJ) — Implementation Record | Final | `main` / uncommitted (base `dbc4765`) | [0016_2026-08-12_DAILY_TRADING_WORKSPACE_MVP_IMPLEMENTATION.md](0016_2026-08-12_DAILY_TRADING_WORKSPACE_MVP_IMPLEMENTATION.md) |
| 0017 | 2026-08-13 | Implementation | Trade Capture & Decision Recording (Milestone BK) — Implementation Record | Final | `main` / uncommitted (base `dbc4765`) | [0017_2026-08-13_TRADE_CAPTURE_AND_DECISION_RECORDING_IMPLEMENTATION.md](0017_2026-08-13_TRADE_CAPTURE_AND_DECISION_RECORDING_IMPLEMENTATION.md) |
| 0018 | 2026-08-14 | Implementation | Portfolio Intelligence & Risk Engine (Milestone BL) — Implementation Record | Final | `main` / uncommitted (base `dbc4765`) | [0018_2026-08-14_PORTFOLIO_INTELLIGENCE_AND_RISK_ENGINE_IMPLEMENTATION.md](0018_2026-08-14_PORTFOLIO_INTELLIGENCE_AND_RISK_ENGINE_IMPLEMENTATION.md) |
| 0019 | 2026-08-14 | Hostile Review | Portfolio Intelligence & Risk Engine (Milestone BL) — Hostile Review | Final | `main` / uncommitted (base `dbc4765`) | [0019_2026-08-14_PORTFOLIO_INTELLIGENCE_AND_RISK_ENGINE_HOSTILE_REVIEW.md](0019_2026-08-14_PORTFOLIO_INTELLIGENCE_AND_RISK_ENGINE_HOSTILE_REVIEW.md) |
| 0020 | 2026-08-14 | Implementation | Market Snapshot & Price Integration (Milestone BM) — Implementation Record | Final | `main` / uncommitted (base `dbc4765`) | [0020_2026-08-14_MARKET_SNAPSHOT_AND_PRICE_INTEGRATION_IMPLEMENTATION.md](0020_2026-08-14_MARKET_SNAPSHOT_AND_PRICE_INTEGRATION_IMPLEMENTATION.md) |
| 0021 | 2026-08-15 | Implementation | Position Sizing & Trade Approval Engine (Milestone BN) — Implementation Record | Final | `main` / `b66a88f` | [0021_2026-08-15_POSITION_SIZING_AND_TRADE_APPROVAL_IMPLEMENTATION.md](0021_2026-08-15_POSITION_SIZING_AND_TRADE_APPROVAL_IMPLEMENTATION.md) |
| 0022 | 2026-08-16 | Implementation | Paper Trading & Trade Lifecycle Engine (Milestone BO) — Implementation Record | Final | `main` / `e4195fc` | [0022_2026-08-16_PAPER_TRADING_AND_TRADE_LIFECYCLE_IMPLEMENTATION.md](0022_2026-08-16_PAPER_TRADING_AND_TRADE_LIFECYCLE_IMPLEMENTATION.md) |
| 0023 | 2026-08-18 | Implementation | Statistics & Performance Engine (Milestone BP) — Implementation Record | Final | `main` / `e1cfad0` | [0023_2026-08-18_STATISTICS_AND_PERFORMANCE_ENGINE_IMPLEMENTATION.md](0023_2026-08-18_STATISTICS_AND_PERFORMANCE_ENGINE_IMPLEMENTATION.md) |
| 0024 | 2026-08-19 | Implementation | Stable Setup Identity (Milestone BG-D1) — Implementation Record | Final | `main` / `2da08b9` | [0024_2026-08-19_SETUP_IDENTITY_IMPLEMENTATION.md](0024_2026-08-19_SETUP_IDENTITY_IMPLEMENTATION.md) |
| 0025 | 2026-08-19 | Implementation | Setup Identity Pipeline Integration (BG-D1b) — Implementation Record | Final | `main` / `2da08b9` | [0025_2026-08-19_SETUP_IDENTITY_PIPELINE_INTEGRATION.md](0025_2026-08-19_SETUP_IDENTITY_PIPELINE_INTEGRATION.md) |
| 0026 | 2026-08-19 | Implementation | Setup Identity Surface Integration (BG-D1c) — Implementation Record | Final | `main` / `2da08b9` | [0026_2026-08-19_SETUP_IDENTITY_SURFACE_INTEGRATION.md](0026_2026-08-19_SETUP_IDENTITY_SURFACE_INTEGRATION.md) |
| 0027 | 2026-08-20 | Implementation | Setup Evidence (Milestone BR) — Implementation Record | Final | `main` / `2059ca7` | [0027_2026-08-20_SETUP_EVIDENCE_IMPLEMENTATION.md](0027_2026-08-20_SETUP_EVIDENCE_IMPLEMENTATION.md) |
| 0028 | 2026-08-20 | Implementation | Setup Evidence (Milestone BR) — Release-Gate Fixes | Final | `main` / `f2cacf5` | [0028_2026-08-20_BR_RELEASE_GATE_FIXES.md](0028_2026-08-20_BR_RELEASE_GATE_FIXES.md) |
| 0029 | 2026-08-20 | Implementation | Swing Decision Workspace v1 (Milestone BS) — `fmits workspace` | Final | `main` / `b6a456c` | [0029_2026-08-20_SWING_DECISION_WORKSPACE_IMPLEMENTATION.md](0029_2026-08-20_SWING_DECISION_WORKSPACE_IMPLEMENTATION.md) |
| 0031 | 2026-08-23 | Implementation | Macro & Cross-Asset Context Foundation (Milestone BU) — `fmits macro` | Final | `main` / `2b30e38` (base `7fbe611`) | [0031_2026-08-23_MACRO_AND_CROSS_ASSET_CONTEXT_IMPLEMENTATION.md](0031_2026-08-23_MACRO_AND_CROSS_ASSET_CONTEXT_IMPLEMENTATION.md) |
| 0030 | 2026-08-22 | Implementation | Global Market Pulse Foundation (Milestone BT) — `fmits pulse` | Final | `main` / uncommitted (base `8ecd822`) | [0030_2026-08-22_GLOBAL_MARKET_PULSE_IMPLEMENTATION.md](0030_2026-08-22_GLOBAL_MARKET_PULSE_IMPLEMENTATION.md) |
| 0032 | 2026-08-24 | Implementation | FMITS Operator Dashboard V0 (Milestone BV) — `fmits dashboard` | Final | `main` / `0f31293` (base `1a73cf8`) | [0032_2026-08-24_OPERATOR_DASHBOARD_V0_IMPLEMENTATION.md](0032_2026-08-24_OPERATOR_DASHBOARD_V0_IMPLEMENTATION.md) |
| 0033 | 2026-08-25 | Implementation + Research | Swing Strategy Laboratory & Historical Replay (Milestone BW) — `fmits research`, `/lab`; the 1W gate measured | Final | `main` / uncommitted (base `d8e1b9e`) | [0033_2026-08-25_SWING_STRATEGY_LABORATORY_IMPLEMENTATION.md](0033_2026-08-25_SWING_STRATEGY_LABORATORY_IMPLEMENTATION.md) |
| 0034 | 2026-08-25 | Implementation + Research | Swing Trade Geometry Research & Policy Candidates (Milestone BX) — `fmits research geometry`, `/geometry`; 13 geometries measured, **no candidate** | Final | `main` / uncommitted (base `f5d59df`) | [0034_2026-08-25_SWING_TRADE_GEOMETRY_RESEARCH.md](0034_2026-08-25_SWING_TRADE_GEOMETRY_RESEARCH.md) |
| 0035 | 2026-08-26 | Implementation + Research | Pre-Registered Swing Geometry Validation & Execution Mechanics (Milestone BY) — sealed pre-registration `a81b6ab8…`, `fmits research validation`, `/validation`; 11 hypotheses on development/validation/holdout, **hypothesis refuted, no candidate** | Final | `main` / uncommitted (base `3598b4c`) | [0035_2026-08-26_PREREGISTERED_SWING_GEOMETRY_VALIDATION.md](0035_2026-08-26_PREREGISTERED_SWING_GEOMETRY_VALIDATION.md) |
| 0036 | 2026-08-26 | Implementation + Research | Swing Thesis Persistence & Exit Mechanics (Milestone BZ) — sealed pre-registration `4d089ff4…`, `fmits research persistence`; post-entry thesis persistence measured and confirmed, 5 exit families on 2 geometries × 3 samples, **all REJECTED, no candidate** | Final | `main` / uncommitted (base `fd1270b`) | [0036_2026-08-26_SWING_THESIS_PERSISTENCE_RESEARCH.md](0036_2026-08-26_SWING_THESIS_PERSISTENCE_RESEARCH.md) |
| 0037 | 2026-08-27 | Implementation + Research | Swing Admission Edge vs Random-Entry Null (Milestone CA) — sealed pre-registration `910cad28…`, `fmits research admission`; 5 matched null families on development/validation/holdout, **NO_EDGE, no candidate**; independent review withdrew the draft's gate-ladder claim and established that the study is underpowered for its own sealed bar, so `NO_EDGE` means "no edge visible at 155 admissions" | Final | `main` / uncommitted (base `21dac05`) | [0037_2026-08-27_SWING_ADMISSION_NULL_MODEL_RESEARCH.md](0037_2026-08-27_SWING_ADMISSION_NULL_MODEL_RESEARCH.md) |
| 0038 | 2026-08-28 | Implementation + Research | Statistical Power & Research Design Foundation (Milestone CB) — `fmis.research_design`, `fmits research design`; prospective/post-hoc separated structurally, cluster-aware resolution, growth-path planning. CA's ~4,800 estimate **REPRODUCED** (4,827) and shown to hold only on the more-symbols path: with 15 symbols fixed the half-width has a floor of 0.33–0.53 ATR and **more years cannot resolve +0.10 ATR at any size**. Verdict `UNDERPOWERED`, binding dimension `cluster_count`; ~467 symbols required | Final | `main` / uncommitted (base `d1735bb`) | [0038_2026-08-28_STATISTICAL_POWER_AND_RESEARCH_DESIGN.md](0038_2026-08-28_STATISTICAL_POWER_AND_RESEARCH_DESIGN.md) |
| 0039 | 2026-08-28 | Implementation + Research | Universe Feasibility & Information Expansion (Milestone CC) — `fmis.universe`, `fmits research universe`, sealed pre-registration `ef6f3950…`; read-only `exchangeInfo` discovery added to the existing Binance adapter. 3,649 instruments → 660 economic assets → **38 eligible**, against CB's 467 required. Crypto co-moves heavily (mean pairwise r 0.604, market factor 62.5 % of variance) but the residual after removing that factor is **indistinguishable from independence**, so dependence was NOT the wall: **598 of 622 depth exclusions are FMITS's own 1,750-day weekly warm-up**, and zero instruments were lost to data quality or liquidity. A post-hoc ceiling over the provider's ENTIRE history reaches only 106 clusters / 1,089 admissions against 467 / 4,823. Verdict **`INFEASIBLE`**, binding constraint `cluster_count`; the feasible question today is ~0.3–0.5 ATR, not 0.10. Universe `PARTIALLY_SURVIVORSHIP_AWARE`. **Independent review performed** (3 reviewers, 16 findings, 2 rejected with evidence): it found the sealed residual scenario is structurally unable to measure residual dependence (demeaning pins it at −1/(K−1) for ANY true value), a latent path to inverting a design parameter from the protected holdout, an ordering control that could not detect an expectancy sort, and a claimed control that did not exist — all fixed, with 39 regressions and 10/10 rule mutants killed. A pre-commit release audit then found 5 more (chiefly that the 0.0023 sensitivity is MARGINAL — 5.9 % over break-even and flips under CB-6's own ±30 % scatter — so only the CONDITIONAL claim is asserted); all fixed. **No measured figure moved and the verdict is unchanged** | Final | `main` / uncommitted (base `c320525`) | [0039_2026-08-28_UNIVERSE_FEASIBILITY_AND_INFORMATION_EXPANSION.md](0039_2026-08-28_UNIVERSE_FEASIBILITY_AND_INFORMATION_EXPANSION.md) |
| 0040 | 2026-09-03 | Research (measurement) | Paired-Effect Dependence Measurement (Milestone CD) — `fmis.paired_dependence`, `fmits research dependence`, sealed pre-registration `28f8ebed…`. Answers the question Milestone CC's estimator structurally could not, and then has **two of its own central claims overturned by independent review**. **First persisted the Milestone BZ capture, which had never been written down** — the missing file behind CB-1, CB-2 and CC-1 — and with it **2,390 observation-level paired differences**. A fresh replay reproduced Milestone CA's counts **and its effects to four decimal places** (246/234 candidates; 155/91/234 matched; −0.2028/−0.3347/−0.5337). One-way random-effects ICC on two named axes: **`rho_w` = −0.0355 [−0.0774, −0.0064]**, bounded near zero, so clustering by symbol costs no information; **`r_b` = +0.1984 [−0.0491, +0.4181]**, positive in **all 15 panels** (+0.095…+0.549) and **all 16 sensitivity cells** (+0.122…+0.514). **CC's central assumption is wrong** — the paired difference does not remove the market factor — but **CD's own first explanation was wrong too and was withdrawn**: the matching radius is a *ceiling* (10–90 days, not 90–360), two families draw a *same-bar* control, and the real cause is that `control_forward` averages 200 draws carrying 2.8 % of the admission leg's variance, leaving `corr(D, admission) = +0.987`. Review also found the **effective-cluster arithmetic overstated ~3×** (arbitrated by simulation: true design effect 1.4266 vs the sealed formula's 3.7446); corrected, the design-relevant `q · r_b` is **+0.037**, still **17×** the `1/K*` = 0.002141 at which the requirement stops being finite, and CC's 38 eligible assets supply **8–23** effective clusters against 467. Verdict **`INCONCLUSIVE`** — the interval spans zero, so 15 assets cannot separate 'no penalty' from 'unreachable at any size'; 5 of 15 panels (all of validation) are **`UNREACHABLE`** outright. **43/43 mutation probes killed** over two campaigns. **Independent review performed and it changed the science**: 3 reviewers, **30 findings, 2 critical**, every one reproduced before acceptance; **9 defects fixed** including a weighted-ICC bug, an observer hook that could have mutated CA's published effect, and **two architecture guards CD had advertised as a strengthening that were circular**; **5 sealed findings disclosed rather than fixed**, each pinned by a test. CA's `NO_EDGE`, CB's `UNDERPOWERED` and CC's `INFEASIBLE` all stand; **CD removes the premise of CC's warm-up-sensitivity recommendation**, and withdrew its own first replacement for it | Final | `main` / one commit on top of `f47cd05` | [0040_2026-09-03_PAIRED_EFFECT_DEPENDENCE_MEASUREMENT.md](0040_2026-09-03_PAIRED_EFFECT_DEPENDENCE_MEASUREMENT.md) |
| 0041 | 2026-09-04 | Implementation (product reliability) | Dashboard Startup Regression — Diagnosis, Repair and Startup Smoke Test. **`fmits dashboard` started, served a correct page, and still could not be opened.** The first request performed all four live engine reads before writing a single byte of HTTP — 37.9 s measured — and a browser given an accepted connection that stays silent that long reports the server as having stopped responding. A **dormant defect since Milestone BV**, deliberate and documented (*"the first refresh happens on the first request, not here… the owner sees the page assemble"*), whose assumption fails because server-side HTML is written once and there is no progressive render to watch. **14,049 tests missed it because every piece was covered and the sequence was not**: the server suite binds a real socket but with a stub refresher, and the CLI suite never serves at all. Repair: the refresh is paid before the URL is announced (`serve(warm=…)`, decided by the command, not the mechanism), with a two-part banner so the terminal is never silent — **37.9 s → 1.7 ms** for the operator's first page load. New **startup smoke test**: a subprocess runs the real `cli.main(["dashboard", …])`, waits for readiness, GETs the main route, asserts the page is recognisably FMITS, and stops it with a real `SIGINT`; only the three networked reads are replaced, by fixtures, so it runs offline in 4.8 s. **Non-vacuity proved by running it**: the repair was reverted and 3 of 6 tests failed. Product-state assessment recorded — 0 broken, 4 stale, 6 unrepresented backend capabilities, 5 UX debts; **EP-21 recommended next** | Final | `main` / uncommitted (base `1a133b1`) | [0041_2026-09-04_DASHBOARD_STARTUP_REGRESSION_REPAIR.md](0041_2026-09-04_DASHBOARD_STARTUP_REGRESSION_REPAIR.md) |
| 0042 | 2026-09-04 | Implementation (product) | Swing Product Slice 1 — The Symbol Decision Surface. **The engine already knew why every symbol was waiting; the workspace threw it away one layer before the screen.** `no_trade_groups` was the only path a `WAIT` symbol had into the workspace and it folds each one into `NoTradeGroup(reason, classification, symbols)` — a symbol arrives as *a name inside a list of names*, under one sentence shared with every other symbol that reached the same conclusion. Its regime reading, its three directional families, its sufficiency, its `as_of` and its whole evidence report reached the workspace nowhere at all, so `/swing/BTCUSDT` answered *"not an actionable or waiting setup"* for a symbol the engine had fully assessed. Slice 1 adds a narrow product projection — `SymbolDecision` → `SwingWorkspace.decisions` → `SymbolDecisionRow` → renderer — carrying `SetupAssessment`'s and `SetupEvidenceReport`'s own fields **item by item rather than as counts**, including `correlated_with` and `independence_note`, so correlated observations are marked *not independent* instead of reading as corroboration. `/swing` gains a per-symbol table; `/swing/SYMBOL` resolves for every assessed symbol and renders decision, regime, directional families and evidence. **Policy proven unchanged**: 81 fixtures through the full composition root, canonicalised as complete `repr()` renderings, **byte-identical** before and after (`096a575a…`). **40 new tests**, all offline, including no-ranking field guards and non-vacuity proved directly against the three unmodified pre-Slice-1 carriers. Live verification reproduced the audit exactly: **BTC** — two of three families lean long, rejected by the weekly regime gate before the tally; **BNB** — cleared that gate, then weekly `sustained_lower` against daily `sustained_higher`. Per-view `as_of` and the setup/execution regimes deferred to Slice 2 | Final | `main` / uncommitted (base `666723b`) | [0042_2026-09-04_SWING_SYMBOL_DECISION_SURFACE_SLICE_1.md](0042_2026-09-04_SWING_SYMBOL_DECISION_SURFACE_SLICE_1.md) |
| 0045 | 2026-09-06 | Investigation and repair (engineering reliability) | Dashboard Shutdown Reliability Gate — the two order-dependent smoke failures. **Report 0044 left two startup smoke tests failing after `SIGINT` and believed them order-dependent ("only after ~4,650 tests") with a high `RLIMIT_NOFILE` suspected. Both beliefs were confounds.** The suite's ordering is not involved, no test leaks state, no descriptor leaks, no thread or child process survives, and `serve()`'s shutdown path was never at fault and is unchanged. **Root cause: a `SIG_IGN` signal disposition is inherited across `exec`, and `subprocess` does not restore it.** A shell without job control gives a background job `SIGINT`/`SIGQUIT` ignored; `restore_signals=True` covers only the three signals CPython itself ignores (`SIGPIPE`, `SIGXFZ`, `SIGXFSZ`); CPython does not override an inherited `SIG_IGN`. So a dashboard spawned from a **backgrounded** pytest could not receive `SIGINT` at all — and only long runs get backgrounded, which is what made it look like ordering. Proved by reducing the reproducer from 4,660 tests to **7**: the smoke file alone, `2 failed / 65.85 s` under `SIG_IGN`, `7 passed / 5.26 s` in a terminal; and in both directions on `RLIMIT_NOFILE` — `ulimit -n 4096` still fails, `1048576` still passes. The hung child was captured live: single-threaded, 9 descriptors, no child process, stack in `serve_forever`'s `poll()` — *exactly where a healthy dashboard sits*, unmoved by a second `SIGINT` 60 s later. Reproduced identically at `77b956d` against its own `src`, confirming 0044's *predates Slice 3* while correcting its reasoning. **Fix at the harness layer**: `dashboard_smoke_driver` restores its own `SIGINT` handler in `__main__` — in the child, not `preexec_fn` (unsafe with the harness's pump threads); **`src/` deliberately untouched**, since `fmits dashboard &` ignoring Ctrl-C is correct POSIX. **156 added lines, 0 deletions, two test files.** Two tests added (14,531 → **14,533**): a root-cause reproducer that fails **in a plain terminal** pre-fix, and a three-cycle start/stop contract asserting exit 0, a re-bindable port and a 5 s shutdown budget against a measured 23 ms. **No timeout raised, no sleep, retry, `xfail`, `skip`, reorder or `SIGKILL`-as-shutdown.** Full suite **14,533 passed in 11:18** under `-W error` in the exact formerly-failing `SIG_IGN` session, plus a normal-session run; eleven reruns of the failing scenarios, zero failures. Real command: ready after the genuine 37.8 s warm refresh, first HTTP **5.3 ms**, Ctrl-C → exit **84 ms** code 0, port released — report 0041's repair intact. Policy per-fixture digests all match, 72 WAIT / 9 CANDIDATE; **the aggregate `096a575a…` quoted by reports 0042–0044 is recorded as not reproducible — its formula was never committed** (§11.6 states a reproducible one). Operator instance PID 74079 never stopped or signalled | Final | `main` / uncommitted (base `71a92e3`) | [0045_2026-09-06_DASHBOARD_SHUTDOWN_RELIABILITY_GATE.md](0045_2026-09-06_DASHBOARD_SHUTDOWN_RELIABILITY_GATE.md) |
| 0046 | 2026-09-06 | Audit and implementation (product milestone) | Swing Product Slice 4 — Risk & Trade-Planning Foundation. **The risk engine was finished and the owner could not reach any part of it.** A read-only audit of ~8,300 lines across `fmis.risk`, `fmis.portfolio_risk`, `fmis.position_sizing`, `fmis.portfolio`, `fmis.valuation` and `fmis.money` found the code correct and already satisfying almost every capital-preservation principle — and found **two breaks at the edges**. (1) **No producer:** `RiskBudget(` and `RiskLimit(` appeared **nowhere in `src/`**, only in four test files, so `per_trade_ceiling` was never consulted, `fraction_for` never resolved and the whole chain was unreachable by construction. (2) **No consumer:** `swing_workspace` put `approval_note` in `workspace.metadata` and `swing_view` read that mapping nowhere, while `SymbolDecisionRow` carried no risk field at all — proved live: `/swing/BTCUSDT` returned four panels and zero risk content. **`SPEC` §8.1's 2 % ceiling lived in no code**, and could not live in `fmis.risk` or the computing half of `fmis.position_sizing`, both AST-guarded to hold no literal beyond 0 and 1. **Two ADRs**: [ADR-0029](../docs/adr/ADR-0029-money-and-numeric-semantics.md) ratifies the implemented `Decimal`/`float` boundary (money exact and asset-tagged, market OHLCV stays `float`, `exact_from_market_price` the one crossing) **changing no production code**; [ADR-0030](../docs/adr/ADR-0030-risk-policy-declaration-boundary.md) makes `fmis.risk_policy` the owner's declaration boundary and **the only producer of a `RiskBudget` in the repository**. The ceiling is **structural** — a declaration above 2 % cannot be constructed — exact on both sides (`1.9999999999 %` in, `2.0000000001 %` out) and **never a default**: `default_below_ceiling` stays `Absent`, so an undeclared fraction resolves to no fraction rather than to the ceiling. Capital is **declared and `ASSERTED`**, read from `~/.fmits/risk_policy.json` (closed key set, so a credential is refused by name), never inferred and never guessed. **No arithmetic was added** — an AST guard asserts `planning` takes no quotient; every division stays `portfolio_risk.geometry`'s through `PositionSizer.size`, called with `remaining_open_risk=None`. **No geometry invented**: entry is the engine's reference price with its *not an order price* caveat carried onto the page, no stop from ATR, no assumed target. Product: a fifth `/swing/SYMBOL` panel between the families and the audit, plus a `/swing` state column and the note the dashboard used to drop. **`WAIT` symbols get no figures at all** — not a zero. **+1,196 production lines in one new package, +1,913 test lines, 165 new tests.** Six hand-calculated vectors (long, wider stop, short, exact ceiling, 0.00004 price, 250k capital) with every expectation a literal; **12/12 targeted mutants killed** — M10 (*a missing money figure becomes zero*) **found a real hole**, closed by a test for the sized path. Non-vacuity pins `_constructions_of("RiskBudget") == {"risk_policy/planning.py": 1}`, which returned `{}` before. Risk-is-not-confidence enforced three ways: vocabulary guard, import guard, and a whole-record comparison of every decision field with and without a policy — **`WAIT` remains `WAIT`**. Policy non-regression **byte-identical** (`sha256 8b22e6c9…`, 72 WAIT / 9 CANDIDATE); Reliability Gate intact, smoke driver untouched; Scan Memory unchanged, no write path added. Risk layer is **sub-millisecond** (`plan_for` 49.6 µs; `/swing` render +0.00 ms). Two existing tests changed: both pinned a **panel index** to express an ordering invariant, and now state the relation (decision first, audit last). Live on 8799; **no live candidate fabricated** (the market produced only `WAIT`) and `~/.fmits/risk_policy.json` **not created** — declaring capital remains the owner's decision. Handoff restarted the operator instance deliberately (production code changed): **74079 -> 18359** on `127.0.0.1:8787`, stopped by its own exact PID, no `pkill`. **`SIGINT` did not stop it** — report 0045's root cause seen in production: a backgrounded instance inherits `SIG_IGN` and the kernel discards the signal; `SIGTERM` ended it in under 2 s. Not a defect and nothing changed for it, but the operational consequence is now recorded. Two process slips recorded rather than smoothed over: a dev dashboard run without an isolated scan-history root (two scans appended to the owner's buffer, nothing evicted), and two full suites left running concurrently into one file whose stale `14692 passed` was caught by the collection arithmetic, not the exit code. **A resumed session then found one more real defect**: an earlier copy fix had landed on one side of a seam, so every `WAIT` panel read *"risk evaluation**:** The engine states…"* — a colon introducing a capitalised sentence — and the renderer's unreachable fallback had no terminal stop at all. Both sides repaired; the **joined** sentence is now pinned by two tests rather than the pieces, since pinning the pieces is what let a half-landed fix pass, and they were proved non-vacuous against a restored defective copy. **Authoritative full suite: 14,533 → 14,699 passed, 0 failed, 0 skipped, 0 warnings** under `-W error` (669.95 s), run after the last edit with no other pytest process alive; the count was predicted (14,533 + 165 + 1) and matched exactly | Final | `main` @ `d1a9e9e` (production+tests) + `386e864` (docs) + a follow-up commit carrying the copy fix and this record, all pushed (base `03dd4e0`) | [0046_2026-09-06_SWING_RISK_AND_TRADE_PLANNING_SLICE_4.md](0046_2026-09-06_SWING_RISK_AND_TRADE_PLANNING_SLICE_4.md) |
| 0047 | 2026-09-07 | Audit and architecture gate (read-only) | Technical Analysis Capability Audit & Architecture Gate — **read-only; `src/` and `tests/` byte-identical to `66bab74`; no engine implemented, no policy changed.** Baseline verified independently: `HEAD`/`main`/`origin/main` all `66bab74`, tracked tree clean with exactly the 16 pre-existing untracked research documents, full suite **14,699 passed / 0 failed / 0 skipped / 0 warnings** under `-W error` (689.62 s), policy non-regression recomputed outside the test suite to **`sha256 8b22e6c9c5e346cb8f62008325b9b0304ecae9af5e0fe46eec4a6c5aa428059c`**, 81 fixtures, 72 WAIT / 9 CANDIDATE, Reliability Gate 9/9. Operator instance PID 46403 on `127.0.0.1:8787` confirmed listening and **never stopped, signalled or requested**; no development dashboard was started; `pkill` not used. **The finding: FMITS has an excellent price-structure spine and almost no technical-analysis body — and roughly half of the owner's own BTC observation is already computed and thrown away.** Six packages (`features/{trend,momentum,volatility,market_structure,support_resistance,pattern_detection}`, 110 lines total) are docstring-only placeholders with `__all__ = []` and zero math. Above L4 there is **no** zone, clustering, touch count, role, level lifecycle, trendline, channel, breakout/retest/acceptance/reclaim concept, impulse/consolidation/range primitive, chart pattern, candlestick engine, divergence, EMA slope, MACD histogram ROC, RSI slope, Fibonacci or Elliott. Fibonacci and Elliott are confirmed **genuinely new scope** — named in no approved source. **Seven information-loss seams traced, and the biggest is a consumer problem, not a computation problem.** `fmis.level_crossing` derives a nine-way classification (`TOUCH`/`WICK_BREACH`/`CLOSE_BREACH` x `WITHIN_RANGE`/`GAPPED_BEYOND`/`ALREADY_BEYOND`) per candle per level and it is rendered as **one integer count**; all three `FeatureSet`s (ema_20/50/200, rsi, atr, macd, relative volume) reach **no operator surface at all**; the evidence report is built for the **SETUP role only**, so 1W and 4H indicators are never even classified; CHoCH, nearest levels, **context-role levels** and setup-role breaks all die at `build_setup_inputs` (75 lines). Indicators return the **latest scalar only** — `ema_series()` computes the whole series and keeps `[-1]` — so no slope, ROC or divergence is derivable in principle (review R5, open since 2026-07-24). **Independence, measured:** no two of the policy's three directional families are family-disjoint (`standing_family_note()` fires on every page; Milestone AW measured 75–79% agreement), so **more indicators add no independence — a new *family* does.** That is the architectural argument for the recommended slice. **Recommendations:** Fibonacci **RESEARCH FIRST** (cheap decisive test named); Elliott **DEFER**, on a stronger ground than the owner's own — a count re-labels as the market develops and is therefore *structurally incompatible* with the prefix-stability contract every L4/L5 engine holds; triple/V/rounded tops **REJECT** (a touch count and a pivot); H&S, symmetrical triangles and wedges **DEFER**; rectangles are not a pattern but a range. Pattern lifecycle simplified from the brief's seven states to **five** (`MATURE` dropped as a score in an enum's clothing). Brief's 15-step order improved in three places: the seam and `compute_series()` added ahead of everything, phases moved **ahead of** trendline geometry (anchor scoping: 10,153 unconstrained candidates per side per view at 500 bars), opportunity projection moved **up** to step 7. **Measured scaling:** levels ~= 0.28 x candles, so `derive_level_crossings` is **quadratic in candles** — 2.2 ms / 10.5 ms / 40.2 ms / 133.3 ms at 200 / 500 / 1,000 / 2,000 bars. Invisible at the production 500-bar window (~0.63 s per 20-symbol scan against a ~38 s fetch); real under replay or a larger universe. Named, not fixed. **Recommended first slice — 'Where price stands, and what it is waiting for'**: widen the `build_setup_inputs` seam + `fmis.price_zones` (zones, seven interaction kinds, role **from interaction history, never position**) + zone facts on the fact sheet and dashboard + a `MissingConfirmation` referencing a real zone + the first `MARKET_STRUCTURE` evidence descriptor. `WATCH_LONG` deliberately **deferred out of the slice** because naming a side is an ADR-0028 decision. **Two ADRs required before implementation** (0031 price-zone semantics and the ATR-derived tolerance boundary, which is a deliberate scoped weakening of ADR-0013's no-tolerance rule; 0032 fact carriage across the setup input boundary), **not five** — an ADR to record the Elliott deferral is named as bureaucracy and refused. **Seven owner decisions listed, three blocking.** Twenty numbered research questions, none answered; **no threshold invented anywhere**. The single most important new test proposed: a hard assertion that **no absence of a Tier-B phenomenon can ever map to `CONFLICTING`**. Final section written for the owner in non-engineering language | Final | `main` @ `66bab74` (report only; no code change) | [0047_2026-09-07_TECHNICAL_ANALYSIS_ARCHITECTURE_GATE.md](0047_2026-09-07_TECHNICAL_ANALYSIS_ARCHITECTURE_GATE.md) |
| 0048 | 2026-09-16 | Documentation / project continuity (milestone) | FMITS Project Memory & Documentation Gate — **documentation only; `src/` and `tests/` byte-identical to `66bab74`, proved by an empty `git diff -- src tests` at start and end; the full suite deliberately NOT re-run and no "suite green" claim made anywhere.** Baseline verified independently: `HEAD` = `main` = `origin/main` = `git ls-remote` = `66bab74`, `0/0` ahead/behind, no stash, no active Git operation. Operator instance **PID 46403** on `127.0.0.1:8787` confirmed listening and **never stopped or signalled**; `pkill` not used; `~/.fmits/risk_policy.json` **not created**. **The failure being fixed: `CURRENT_STATE.md` was not merely stale but self-contradicting** — a banner accurate to 2026-09-06 above a section literally titled *Current milestone* still naming **AV** from August, a `Test count` of **10,671** against a true **14,699**, a `Repository status` claiming Milestone AP was unpushed, an `Immediate next milestone` still discussing AN/AO/AP, and a roadmap listing dashboards, paper trading and persistence as *Deferred* after all three had shipped. `START_HERE_FOR_AI.md` still described *five CLI commands* against a live **26** and *27 ADRs* against **30**. **Four documents created, each for a responsibility the tree was searched for and found to lack**: `CAPABILITY_REGISTRY.md` (the canonical status index — twelve states never collapsed, because `PRODUCT_UNREACHABLE` vs `MISSING`, `DORMANT` vs `PRODUCT_UNREACHABLE` and `CANDIDATE` vs `MISSING` are exactly the distinctions that were being lost); `docs/reviews/REPORT_0047_REVIEW_DISPOSITION.md`; `CHATGPT_PROJECT_INSTRUCTIONS.md`; and `docs/AI_HANDOFF/daily/` with a template and the first handoff. `START_HERE_FOR_AI.md` **rewritten** as a thin router carrying the source-authority hierarchy and the startup/shutdown ritual; `CURRENT_STATE.md` **restructured, not truncated** — a maintained **§0** block above a ~3,200-line archive, with six silently-stale sections given in-place banners and **no history deleted**. **The rule established: DEFERRED ≠ FORGOTTEN** — every deferral carries why, whether still desired, what must exist first, and its revisit trigger. **Report 0047 recorded as APPROVED WITH REQUIRED ARCHITECTURAL MODIFICATIONS — twelve of them** (Opportunity ≠ Strategy, so `WATCH LONG` + `WAIT` *and* `WATCH LONG` + `CANDIDATE` both stay possible; `MissingConfirmation` ≠ policy `Blocker`; `compute_series()` **before** ATR-based zones; zone-evidence independence **NOT established**; support/resistance role from **interaction history, never position**; and explicit refusals to freeze the phase-segmentation, trendline-anchor and divergence-alignment policies) — **with report 0047 itself left unmodified as point-in-time evidence.** **Twelve registry statuses re-derived from live code, every one reproducing 0047**: the six Tier-2 placeholders measured 18+17+16+20+16+23 = **110 lines, zero math**; `build_setup_inputs` (`swing_setup/compose.py:225`) read in full and confirmed to drop context-role levels, every crossing, every CHoCH, nearest levels, setup-role breaks and all three `FeatureSet`s; `AverageVolume` constructed **nowhere** in `src/` (one grep hit — its own `class` statement); `Fibonacci` and `Elliott` **0 occurrences** in both approved sources and in `src/`; `price_zones`, `trendline` and price/oscillator `divergence` absent, the 12 `divergence` hits all unrelated senses. Backlog/changelog **reconciled without rewriting history**: **§5 NOW satisfied for the first time since `AP` on 2026-08-06**, with the honest six-week gap preserved in a collapsed block stating its paragraphs were true when written; the `DV` label collision recorded rather than silently renamed; **no changelog entry added, deliberately**, since this changed no user-visible capability. **No ADR created** — creating one to restate a report was named as bureaucracy and refused. **No production code, no tests, no TA engine, no `compute_series()`, no zones, no `WATCH`, no policy change, no capital configured**, and the 16 pre-existing untracked research documents untouched. Next milestone: **TA Slice 5A — Recover Technical Context**, pending Dovydas + ChatGPT review | Final | `main` @ `66bab74` (documentation only) | [0048_2026-09-16_PROJECT_MEMORY_AND_DOCUMENTATION_GATE.md](0048_2026-09-16_PROJECT_MEMORY_AND_DOCUMENTATION_GATE.md) |
| 0049 | 2026-09-17 | Implementation record (production milestone) | Technical Analysis Slice 5A — Recover Technical Context & Feature Series. **FMITS stopped forgetting what it already knows.** Baseline verified independently: `HEAD` = `main` = `origin/main` = `4d77e4d`, `0/0` ahead/behind, no stash, no active Git operation, the 16 pre-existing untracked research documents present and **untouched at close**; policy digest recomputed outside the suite to `8b22e6c9…`, 81 fixtures, 72 WAIT / 9 CANDIDATE. Operator instance **PID 46403 on `127.0.0.1:8787` confirmed `LISTEN` and never stopped, signalled or requested**; development on port **8799**; `pkill` not used; `~/.fmits/risk_policy.json` **not created**; `~/.fmits/scan_memory` never written. **The finding acted on:** `build_setup_inputs` receives three complete `StructuralFactSheet`s and emits twenty-two scalars and two level tuples — report 0047 §7's table reproduced exactly against live code, with a per-fact determination of type, immutability, provenance, existing consumer and whether the product needs it now. **Two ADRs, both taken before implementation. [ADR-0031](../docs/adr/ADR-0031-feature-series-contract.md)** — the **additive** feature-series contract, closing review item **R5 open since 2026-07-24**. Report 0047 observed that `FeatureValue` already permits `Sequence` so *no contract change is needed*; this records that as **true and not sufficient** — a bare list answers none of alignment, warm-up boundary, undefined-versus-absent, or series identity. `SeriesFeature` is a **second Protocol beside** `Feature`, which is untouched, as is `BaseFeature`; `FeatureSeries` carries the closed-candle index space `SwingPoint`/`LevelOrigin`/`LevelCrossingEvent` already use, one `SeriesIdentity` for the whole series (ADR-0018's convention), `as_of` = last **closed candle** and deliberately not the last point, `first_index`/`latest` as projections (ADR-0016 §4); **warm-up is never backfilled**; a point carries a value **or** an `undefined_reason`, never both and never neither, so *not warmed up* / *undefined here* / *computed* stay three facts (`RelativeVolume` over an all-zero baseline is the live case); MACD emits its three-component mapping per point, proving the protocol is not scalar-only; **one mathematical implementation per indicator** in a shared `*_math` helper that both paths read, so latest-value parity is **bit-identical by construction** and asserted with no tolerance. **No engine-level `compute_series`**, because `FeatureEngine` threads *latest* results into the next feature's context and doing that to a history is lookahead produced by the orchestration — asserted absent. Six features converted; `AverageVolume` gained a history and **stays DORMANT**, with a test that `default_features()` did not grow. **[ADR-0032](../docs/adr/ADR-0032-market-technical-context-carriage.md)** — the carriage boundary. **The obvious repair was refused**: widening `SetupInputs` would make the strategy's own input the warehouse for all TA and invite a later milestone to gate on a recovered fact *because it was in scope* — the correlated-vote inflation the registry §3 measures. `SetupInputs` gained **no field** and `evaluate_setup` is untouched byte for byte; a sibling `MarketTechnicalContext` is composed **after** the assessment from objects the composition already built, generalising the precedent `SetupReadings` set in Slice 1. Owned by **`fmis.pipeline`** (ADR-0022/0023 already own both sheet types) — no new top-level package, and a test asserts the module holds **no arithmetic operator at all**. Canonical objects carried **by reference** (asserted with `is`, element by element), roles never blended, and the **bounded summaries selected beside the full history they come from**, never in a renderer. **Recovered, all three roles:** every `FeatureSet`, every level crossing, every CHoCH, nearest levels, context-role levels, setup-role breaks, setup- and execution-role regimes, warm-up and last close. **One row deliberately left behind** — `swings`/`labelled`/`state_history`, the *inputs* to facts already carried — with a stated revisit trigger. **Product:** a **technical context panel** on `/swing/SYMBOL` — one table row per role for the six facts read in seconds, everything else behind a per-role disclosure. `/swing` untouched. **No raw event flood**: thousands of crossings per role, two rendered plus a truthful count. **Policy non-regression: byte-identical**, recomputed three times outside the suite including on cleared bytecode; the 162-digest per-fixture table passes and its file was not edited. **Scan Memory reaches zero of it**, asserted four ways including whole-record equality, with existing history proven readable. **Verification:** full suite re-established under `-W error` on cleared bytecode; **17/17 targeted adversarial probes killed** with byte-exact source restore; series scaling measured **linear** (0.69/1.94/4.20/8.47 ms at 200/500/1,000/2,000 candles × 7 features); carriage measured at **0.208 ms and 0.9 KiB per symbol** — ~1 % of the 20.6 ms three-view sheet build — with objects **referenced, never copied**; module-level import cycles excluding `pipeline/cli.py`: **one, pre-existing in `paired_dependence` since `1a133b1`**. **Four architecture guards fired and were obeyed rather than widened** — they scan for the *text* of an engine's package name and caught docstrings crediting engines by module path in the dashboard and workspace, which import none of them; the prose was corrected. Two existing tests changed in total, each with its reason recorded. **A campaign-integrity defect found and fixed mid-milestone**, recorded in §19.1 and in `CURRENT_STATE.md` §0.1: a same-length mutation restored inside one second leaves a **stale mutated `.pyc`** behind a correct source file, because CPython invalidates on `(mtime, size)` at one-second resolution — `inspect.getsource` showed the restored source while the interpreter ran the mutation, a `sha256` over `src/` said *restored exactly* and was **true and insufficient**, and it produced one false survivor and would have produced false kills for every later probe. The campaign was rewritten with `PYTHONDONTWRITEBYTECODE=1` and per-probe cache clearing and re-run from scratch. The one genuine survivor it exposed — a shared line/signal pairing shift that moves `compute` and `compute_series` **together**, so parity and prefix stability both keep passing — is closed by two tests that derive the expected value from the prices at that bar. **Stopped where told:** no zones, phases, patterns, divergence, trendlines, opportunity or `WATCH`, and **not one** of the indicator interpretations `compute_series()` now makes easy. A rendered-output scan across three markets refuses nineteen unearned words with the page's single denial sentence carved out and asserted separately. Next milestone: **TA Slice 5B — Price Zones**, **BLOCKED on owner decision 0047 D1** | Final | `main` @ `00c7723` | [0049_2026-09-17_TECHNICAL_ANALYSIS_SLICE_5A.md](0049_2026-09-17_TECHNICAL_ANALYSIS_SLICE_5A.md) |
| 0050 | 2026-09-18 | Research / design record | **FMITS Price Zone Semantics & Parameter Research Gate.** Closes report 0047's **D1** and **D2**. Baseline verified live: `HEAD` = `main` = `origin/main` = `96b56ea`, `0/0`, clean tree, the 16 untracked research documents present and **untouched at close**; operator instance **PID 46403 on `127.0.0.1:8787` confirmed `LISTEN` read-only and never signalled**; `~/.fmits/risk_policy.json` **not created**; `src/` and `tests/` **zero files changed** and the policy digest `8b22e6c9…` untouched. **The finding:** 0047 §43-D1 reads as one decision and is three — the distance scale (§15.3's whole argument), the **grouping geometry** (§15.2 says *"clustered"* and stops) and the temporal reference — and the geometry, which 0047 never discusses, dominates. Preregistration **sealed at `c05e870` before the harness existed** (`sha256 d5a94b05…`), fixing the axes, the candidates with a reason per member, the grids, the dataset, fourteen adversarial fixtures, the admissibility gate and the four verdicts. **55,728 policy cells** over **36 symbols × 3 timeframe roles**, offline from Milestone CD's committed capture `07b500c7…` (no network, no credential). **Single-linkage clustering — the natural reading of *"clustered"* — chains catastrophically**: 247 of 339 levels into one zone on BTC 1D, zones up to **94× the policy's own tolerance**. A diameter-bounded greedy is **not reflection-symmetric** (2 % of cells; a 3-level counterexample is given) and rewrote history on **120,441** prefix events. **Exactly one family survived**: anchored construction, ATR-multiple width **frozen at the anchor's establishment bar**, `k ∈ [0.10, 1.00]` — **0 illegal prefix events**. Width from **current ATR: 983,916** illegal events; letting a joining member **widen the band: 34,192** — §13's two temporal questions answered by measurement, not intuition. `W-PCT` rejected because it is **not long/short symmetric** (6 %) — a stronger and *measured* objection than 0047's *"invented threshold"*; `W-TICK` rejected as **not expressible** (no instrument metadata under `src/`); `W-STRUCT` rejected at 696,588 illegal events. **Three candidate rules were repaired mid-study and every repair is recorded in place** — including that **the preregistration's own tie-break was wrong** (nearest-centre compares two nearly-equal floats and flipped on 35 % of series; replaced by *oldest containing band*), that one candle can be both a swing high and a swing low so a bar must be treated as one instant, and — the methodological point — that **the rejected `W-STRUCT-CAUSAL` was given the identical repairs and re-run** before being rejected, on numerical fragility rather than on whose bug was found first. One harness defect found and fixed: partition identity keyed on `origin_index` merged the two levels one candle can produce; an assertion now raises if any transform drops the real identity, and it caught a second instance immediately. **The parameter is deliberately not chosen.** Every descriptive metric is **monotone** in `k` — no plateau, no optimum, no knife-edge — so `k` is a **resolution control** and a study that refuses outcome data cannot derive one. The region is bounded (`k=0` → 96 % singletons; `k≥2` → 99.4 % merged, 44-member zones; symmetry lost above 1.00) and **V1 must declare `k`, which ADR-0033 says in those words**. **D2 strengthened from an architectural position into a measurement**: **22 %** of zones above the close on 1W have *no interaction history at all* (max 75 %), and **39 % (1W) / 50 % (1D)** have had price close on **both** sides since establishment — so the forbidden position rule is not merely sometimes wrong, it is **not well-defined over time**. Internal vocabulary fixed; *"Support zone"* permitted later only as a label over a derived role and never for `UNTESTED`. **Zone evidence independence remains `NOT ESTABLISHED`** (R15) and no zone reaches evidence voting, the 1W gate, Scan Memory or risk. Determinism verified **byte-identical across two processes and two `PYTHONHASHSEED` values**; **0** production references to the research package; `fmis` imports with `research/` off the path. **[ADR-0033](../docs/adr/ADR-0033-price-zone-semantics-and-the-tolerance-boundary.md) `Proposed`**, plus the design document and the sealed preregistration. Next milestone: **TA Slice 5B**, unblocked on D1/D2 and blocked only on the owner accepting the declaration | Final | `main` @ `96b56ea` | [0050_2026-09-18_PRICE_ZONE_SEMANTICS_RESEARCH_GATE.md](0050_2026-09-18_PRICE_ZONE_SEMANTICS_RESEARCH_GATE.md) |
| 0051 | 2026-09-18 | Implementation record (production milestone) | **Technical Analysis Slice 5B — Price Zone Foundation & Product Surface.** **FMITS sees an area where it used to see three unrelated lines**, and still refuses to say what that area means. Baseline verified live and independently of the brief: `HEAD` = `main` = `origin/main` = remote `refs/heads/main` = `693e162`, `0/0`, clean tracked tree, no stash, no active Git operation, the 16 pre-existing untracked research documents present and **untouched at close**; policy digest recomputed **before** any change to `8b22e6c9…` (81 fixtures, 72 `WAIT` / 9 `CANDIDATE`); operator instance **PID 46403 on `127.0.0.1:8787` confirmed `LISTEN` at start and at close, never stopped, signalled or requested**, `pkill`/`killall` never used; development on port **8799** with an isolated store; `~/.fmits` `sha256`-compared before and after and **byte-identical**; `~/.fmits/risk_policy.json` **not created**. **[ADR-0033](../docs/adr/ADR-0033-price-zone-semantics-and-the-tolerance-boundary.md) moved `Proposed` → Accepted**, carrying the owner's four declarations as an acceptance record that adds to the document without rewriting one research claim: **`k = 0.50` is declared, not discovered** — not an optimum, not a calibrated threshold, not an edge claim; **one `k` for 1W/1D/4H** with no per-role constant; and *"Support zone"* approved as a **future** label over a derived role, explicitly **not** permission to build the interaction engine. **One contradiction between binding sources, resolved in the ADR's favour**: the backlog and the registry both name the next milestone *"Price Zones & Interactions"*, while ADR-0033 — later and more specific — states `ZoneInteraction`/`ZoneReading` are not V1 and R3/R4 are unanswered. The planning documents were corrected, not the ADR. **Shipped: `fmis.price_zones`** (1,132 lines, 15 public names, **0 collisions** against the repository's 1,774) — anchored, online construction; the band **frozen at its anchor** (`anchor.price ± 0.5×atr_14/2`, read at the anchor's own establishment bar); ties to the **oldest containing band**; **one bar is one instant**; membership by price alone and inclusive at both edges; **no ATR → no zone**, never a default width; bands overlap and are **never merged**; the `ZoneWidthPolicy` stamped **by value** on every zone; and the admissible region `[0.10, 1.00]` **enforced by the type** rather than documented beside it. **`PriceLevel` equality, `classify_comparison` and `CrossingKind` are untouched** — the tolerance is scoped to band membership alone. **Product: a price zones panel on `/swing/SYMBOL`** under the technical context it groups — one headline row per role (nearest area above, the area price is inside, nearest area below, true count), with the selection around the close, the counts, the width policy and per-band provenance behind disclosures. A real symbol produces **20–250 bands per role**; the page shows at most nine and prints the totals beside them. **Position is geometry and cannot become a role**: `ZonePricePosition` reads as a sentence about the price, there is **no `role` field anywhere**, and **the approved role vocabulary was deliberately left out of the code** — an enum present in the source is one something will populate. **+324 tests, every one new** (15,092 → **15,416**, `-W error`, cleared bytecode, 0 warnings). Prefix stability, no-lookahead, no growable band, oldest-band ties, same-bar epochs, exact `PriceLevel` identity, equal-price-different-origin distinctness, overlap, missing-ATR, determinism (**byte-identical across three subprocesses at `PYTHONHASHSEED` 0/1/12345**), exact unit invariance at ×2/×1024/×0.125 and exact **long/short symmetry at every `k` in [0.10, 1.00]** — all asserted of **production** code over production `structural_levels` and the production ATR, not of the research harness. **Policy non-regression: byte-identical** — `8b22e6c9…`, 81 fixtures, 72/9, recomputed outside the suite before and after; the 162-digest per-fixture file was **not edited**; no zone reaches evidence voting, the 1W gate, Scan Memory or risk, asserted on live objects and by whole-word source scans. **Three architecture guards fired and all three were obeyed rather than widened away**: the level-crossing consumer guard was widened **with the named exemption and ADR justification its own docstring demands**; the market-regime guard caught a docstring spelling a package path the package does not import, and **the prose was corrected**; and the dashboard contract guard — *"`sum`, `min`, `max` and `sorted` are how a presentation layer becomes an engine"* — caught the zone selection being sorted in `sections.py`, so **the ordering moved onto `PriceZoneSet` itself**, with the rendered output verified byte-identical across the move. **Performance measured, not guessed**: **0.55 ms per symbol-role at the live 500-bar window** (≈33 ms for a 20-symbol watchlist), 14.8 ms at a 7,313-bar pathological window; **no cache introduced**. **Live verification on four real markets** — BTCUSDT, ETHUSDT and two sub-dollar altcoins — rendered output read rather than status-checked, overlapping bands and the no-width case both occurring **naturally** and printed honestly, repeated `GET` **byte-identical**, Strategy Decision unchanged, and **zero** of 36 refused words outside the panel's own denial paragraph. **Not shipped, deliberately**: zone interactions, zone readings, any derived role, the role vocabulary itself, support/resistance labels, breakout/acceptance/reclaim/retest/false-breakout, zone strength/quality/score/rank/confidence, directional bias, `WATCH`, patterns, Fibonacci, Elliott — and **no strategy, policy, risk or capital change whatsoever**. Next milestone: **the owner's selection**, between a preregistered outcome-based **R3/R4 research gate** (which unblocks zone roles and every breakout-adjacent concept) and **Price Phases** (unblocked today). This milestone was forbidden to resolve R3/R4 and has no standing to promote its own successor | Final | `main` @ `3739d42` + its documentation commit | [0051_2026-09-18_TECHNICAL_ANALYSIS_SLICE_5B.md](0051_2026-09-18_TECHNICAL_ANALYSIS_SLICE_5B.md) |
| 0052 | 2026-09-20 | Research / design record | **Price Zone Interaction Semantics Research Gate — R3 / R4.** Baseline verified live and independently of the brief: `HEAD` = `main` = `origin/main` = remote = **`527a162`**, `0/0`, clean tracked tree, no stash, no active Git operation, the 16 pre-existing untracked research documents present and **untouched at close**; operator instance **PID 46403 on `127.0.0.1:8787` confirmed `LISTEN` read-only and never signalled**; `~/.fmits/risk_policy.json` **not created**; **`src/` and `tests/` zero files changed**. **Authority reconciliation changed the study**: report 0047 §45 asks R3 for a *"distinguishable state rather than a restatement of a close breach"* and R4 for a return *"attributable to the break"*, while `CURRENT_STATE.md` paraphrases both down to their parameters — run against the paraphrase this gate would have been obliged to return two numbers it had not earned, and **both numbers exist and both are misleading**. Preregistration **sealed and pushed at `296831a` before the harness existed** (`sha256 d98e1516…`), fixing both formulations, the candidate set with a reason per member, the a-priori rejections, the matched-knowledge-time comparison design, the metrics, the cluster unit, the bootstrap seed `20260920`, the minimum-cell rule, 20 fixtures, 10 invariants and **six verdicts with numeric thresholds** (≥ 10 points for R3, hazard ratio ≥ 1.25 for R4). Only a **power audit** preceded the seal, and it predicted the 1W shortfall that later fired. **153,148 real events from 9,881 zones over 36 symbols × 3 roles**, plus **596,578 placebo events**, offline from Milestone CD's capture `07b500c7…` (no network, no credential). **R3 — RESOLVED WITH LIMITATIONS, and not as the question assumed.** A second consecutive close beyond a frozen band moves ten-bar persistence from **0.41 to 0.65** (**+22 to +25 points**, 1D and 4H, primary and holdout, Holm-adjusted p = 5.1e−04); a third adds **+26 to +29** more, so **there is no knee and `N` is a resolution control**, the identical structural conclusion 0050 reached about `k`. Displacement is **complementary, not dominant** — the second close still separates **within every displacement bin** (+0.17 to +0.29), refuting R3-C. **But a POST-HOC placebo — identical width, identical establishment bar, displaced so no structural level anchored it — separates by the same amount: +0.243 against the real +0.246, an excess of ±0.004 that flips sign across cells.** The state is real; **its attribution to the zone is not**, so FMITS may compute it and **must not call it acceptance**. The post-hoc control was **not preregistered and is labelled POST-HOC in the module, the report and every table**; a strict subset holding both groups outside the band at classification shows a *larger* effect (+0.30 to +0.43), ruling out the tautology it might have been. **R4 — NO SUPPORT.** The return hazard to a real zone is indistinguishable from a clean placebo at **every** elapsed range on **every** role — **ratio 0.96–1.02** on 4H and 1D, several bins significantly *below* 1, under both return definitions, all three excursion preconditions and the persistence split. **Stage 2 was not run, by the preregistration's own ordering.** The value of the control: the raw hazard decays seven-fold (**0.206 → 0.121 → 0.070 → 0.046 → 0.027 → 0.015**) and alone reads as a textbook *"retests cluster in 2–3 bars"* finding worth a confident `R4 = 3`; **the placebo decays identically (0.207 → 0.122 → 0.072 → 0.045 → 0.026 → 0.015)** — it is first-passage geometry, not market memory. Attribution is rarely even isolable: only **33.5 %** of 144,303 resolved episodes have no other zone transition in between and **30 %** have ten or more. **Inference never uses the event count** — 22.9 events per zone on primary 4H — but the **symbol** (15 + 21), paired Wilcoxon plus a 2,000-draw cluster bootstrap; the sealed minimum-cell rule fired on **1W in both universes** and those cells are reported `UNDERPOWERED`. **68/68 adversarial fixture assertions** and **27/27 real-data checks** pass: prefix stability, window-start insensitivity, reflection symmetry, scale invariance at ×2/×1024/×0.125, determinism **byte-identical across processes at `PYTHONHASHSEED` 0 and 12345**, and both isolation assertions. **Three defects found and recorded rather than tidied away**: the artifact was **not reproducible** because `gzip.open` stamps the wall clock into the header (a determinism check *asserted* rather than *measured* would have shipped it); **five fixture expectations were wrong and the harness was right** (bar index confused with elapsed bars — the fixtures were fixed, not the code); and two verifier checks were too strict, one treating `ABSENT → 1` at a prefix's final bar as drift when it is history arriving, the other flagging 36 events whose `disp_atr` differs **in the 13th significant figure** because **ATR(14) is a recursive Wilder average with no finite warm-up** — a production property quantified here for the first time, and **no event changes displacement bin**. **Six of ADR-0033's seven approved role labels are underivable**; the only one available is `UNTESTED`, which means nothing has happened. **Support/resistance labels remain unavailable** and the `position` shortcut remains forbidden. **Rejected on evidence:** `RETEST`, `ACCEPTANCE`, `BREAKOUT`, `RECLAIM`, `FALSE_BREAKOUT`, `REJECTION`, any retest bar-window, direction-specific semantics and per-role constants. **Admissible:** `INSIDE` · `OUTSIDE_ABOVE` · `OUTSIDE_BELOW` · `EXIT` · `TRAVERSE` · `RETURN_TO_ZONE_AFTER_OUTSIDE_STATE` · `CLOSED_BEYOND_FOR_N_BARS`. **No policy, strategy, risk or capital change**: the 81-fixture non-regression suite re-run and **4 passed** under `-W error` — and the report records honestly that the `8b22e6c9…` aggregate digest prior reports cite **has no recipe recorded anywhere in the repository**, so non-regression is evidenced by the assertion suite itself rather than by a number that could not be derived. **[ADR-0034](../docs/adr/ADR-0034-zone-interaction-evidence-boundary.md) `Proposed`**, binding the negative boundary so it is not re-litigated by intuition. Next milestone: **Price Phases** — Zone Interactions stays **BLOCKED**, now on evidence rather than on ignorance | Final | `main` @ `527a162` → `296831a` (preregistration) → this milestone | [0052_2026-09-20_PRICE_ZONE_INTERACTION_SEMANTICS_RESEARCH_GATE.md](0052_2026-09-20_PRICE_ZONE_INTERACTION_SEMANTICS_RESEARCH_GATE.md) |
| 0044 | 2026-09-05 | Implementation (product) | Swing Product Slice 3 — Scan Memory and "What Changed". **Slices 1 and 2 answered *what is happening with this symbol now, and why*; nothing answered *what changed since I last looked*.** The Swing scan is twenty symbols and most sit at `WAIT` for days, so a symbol whose **reason** for waiting moved from *the HTF regime gate rejected it* to *the directional families disagree* — a materially different situation — rendered identically to one where nothing happened. The comparison required holding twenty symbols × eight states in the operator's head between sessions. Slice 3 adds `fmis.scan_memory`: a projection of one completed scan into named states, a deterministic comparator over **twelve declared dimensions**, and a bounded rolling store at `~/.fmits/scan_memory` built on the archive's own `atomic_write` — **outside the checkout**, so no ignore rule was needed. **Existing persistence was read first**: `RecordStore` is a money audit trail that never prunes, `ArchiveStore` (ADR-0027) is permanent by definition; only the *primitive* was reusable. **The central invariant** — scan memory observes decisions and never participates in them — is asserted as an import direction for **26 packages** and again functionally: the same workspace renders the same decision against an empty store, a matching baseline and a *contradicting* one. **Time passing cannot be a change**, structurally: no instant, age or bar count is a dimension and per-role instants/ages/bar counts are not persisted at all — proved live by two real scans 83 s apart reporting **zero events**. **Honest absence**: four statuses, and `ScanComparison` refuses at the type level to carry per-symbol results under any but `COMPARED`, so *nothing changed* cannot be said without a real comparison. **No score, no rank** — five arithmetic expressions in the whole package, written out and checked as a set. **Policy byte-identical** (`096a575a…`, the same digest as Slices 1 and 2). **375 new tests** plus a seventh startup smoke test proving the real command records one scan and six page loads record none; two genuine regressions found by the full suite (an export-name collision and a pipeline import-direction breach) fixed at the architecture with no guard weakened. Live: port 8799, operator instance PID 49813 untouched; baseline persisted, survived a real process restart, second scan compared. ~0.5 ms added per refresh. Full suite **14,155 → 14,531 collected**, 14,529 passed / **2 failed** — both **pre-existing**, reproduced identically in a clean worktree at `77b956d` with Slice 3 absent (two startup smoke tests whose subprocess does not exit on `SIGINT` when run after ~4,650 other tests); no fix attempted, recommended as the next dashboard task (§16.1). Alerts, AI interpretation, ranking, freshness policy and all Slice 4 work deferred | Final | `main` / uncommitted (base `77b956d`) | [0044_2026-09-05_SWING_SCAN_MEMORY_SLICE_3.md](0044_2026-09-05_SWING_SCAN_MEMORY_SLICE_3.md) |
| 0043 | 2026-09-04 | Implementation (product) | Swing Product Slice 2 — The Operator Decision Layer. **Slice 1 made the evidence reachable; the operator then reported that the page answered an *audit* question before a *trading* one.** Four facts were derivable and none was a value: which way the readable families lean (three `Lean`s a reader had to compare), which condition stopped the reading (three different English sentences — not something two symbols can be compared on), the two timeframe states, and the data age. On that last one `run_setup_for_symbols` discarded the fact sheet with a literal `_`, so **one instant stood for three timeframes that close at different rates** — measured live, BTCUSDT's page said `as_of 2026-09-04T04:00Z` while the **weekly reading that gates every direction was 11 days 11 hours old**. Slice 2 adds `TimeframeReading` (with `age_at`), `SetupReadings` and a `decision_summary` projection in `fmis.swing_setup` — the ADR-0028 package, because `DevelopingEvidence` names a side — carried on a new defaulted `SetupRunResult.readings` that reaches the workspace with **no change to `fmis.today` at all**. `/swing` gains a scan snapshot and six operator columns; `/swing/SYMBOL` becomes four panels with the trading answer first and the audit last, **complete and unchanged**. **`WAIT` never becomes a signal**: enforced at five points, two of them on the types themselves. **No `FRESH`/`STALE` verdict** — the dormant `fmis.snapshotting` freshness record was inspected and deliberately not activated (it has no producer and measures bar ages at snapshot-freeze), and no validated staleness bound exists for any role, so the page states the age and declines to grade it. **Policy byte-identical** (`096a575a…`, the same digest as Slice 1), with an anti-drift test asserting the named blocker matches the policy's own sentence on all 24 WAIT exits. **53 new tests**, one of which replaced a vacuous one found during review. Full suite **14,102 → 14,155** under `-W error` | Final | `main` / uncommitted (base `77475a5`) | [0043_2026-09-04_SWING_OPERATOR_DECISION_LAYER_SLICE_2.md](0043_2026-09-04_SWING_OPERATOR_DECISION_LAYER_SLICE_2.md) |
| 0013 | 2026-08-11 | Review | Research Harness Correction V1 — Hostile Review | Final | `main` / uncommitted (base `f9ddc54`) | [0013_2026-08-11_RESEARCH_HARNESS_CORRECTION_HOSTILE_REVIEW.md](0013_2026-08-11_RESEARCH_HARNESS_CORRECTION_HOSTILE_REVIEW.md) |
| 0012 | 2026-08-11 | Implementation | Research Harness Correction V1 — Implementation Record | Final | `main` / uncommitted (base `f9ddc54`) | [0012_2026-08-11_RESEARCH_HARNESS_CORRECTION_IMPLEMENTATION.md](0012_2026-08-11_RESEARCH_HARNESS_CORRECTION_IMPLEMENTATION.md) |
| 0011 | 2026-08-08 | Implementation | Swing Setup Historical Backtest Harness V1 — Implementation Record | Final | `main` / uncommitted (base `35bce7a`) | [0011_2026-08-08_SWING_SETUP_BACKTEST_V1_IMPLEMENTATION.md](0011_2026-08-08_SWING_SETUP_BACKTEST_V1_IMPLEMENTATION.md) |
| 0010 | 2026-08-07 | Implementation | Market Scanner Intelligence Report V1 — Implementation Record | Final | `main` / uncommitted (base `81a6202`) | [0010_2026-08-07_MARKET_SCANNER_INTELLIGENCE_REPORT_V1_IMPLEMENTATION.md](0010_2026-08-07_MARKET_SCANNER_INTELLIGENCE_REPORT_V1_IMPLEMENTATION.md) |
| 0009 | 2026-08-07 | Implementation | Market Scanner V1 — Implementation Record | Final | `main` / uncommitted (base `9977274`) | [0009_2026-08-07_MARKET_SCANNER_V1_IMPLEMENTATION.md](0009_2026-08-07_MARKET_SCANNER_V1_IMPLEMENTATION.md) |
| 0008 | 2026-08-04 | Audit | Documentation Consistency Audit | Final | `main` / `36f5a30` | [0008_2026-08-04_DOCUMENTATION_CONSISTENCY_AUDIT.md](0008_2026-08-04_DOCUMENTATION_CONSISTENCY_AUDIT.md) |
| 0007 | 2026-08-02 | Readiness Check | Implementation Readiness Check | Final | `main` / `d132cea` + uncommitted AF | [0007_2026-08-02_IMPLEMENTATION_READINESS_CHECK.md](0007_2026-08-02_IMPLEMENTATION_READINESS_CHECK.md) |
| 0006 | 2026-08-02 | Architecture Gate | Milestone AF Architecture Gate | Final | `main` / `d132cea` + AF | [0006_2026-08-02_MILESTONE_AF_ARCHITECTURE_GATE.md](0006_2026-08-02_MILESTONE_AF_ARCHITECTURE_GATE.md) |
| 0005 | 2026-08-01 | Development Roadmap | FMITS Development Roadmap 2026–2027 | Final | `main` / `d132cea` | [0005_2026-08-01_FMITS_DEVELOPMENT_ROADMAP_2026_2027.md](0005_2026-08-01_FMITS_DEVELOPMENT_ROADMAP_2026_2027.md) |
| 0004 | 2026-08-01 | Business & Capability Architecture | FMITS Business & Capability Architecture V1 | Final | `main` / `d132cea` | [0004_2026-08-01_FMITS_BUSINESS_AND_CAPABILITY_ARCHITECTURE_V1.md](0004_2026-08-01_FMITS_BUSINESS_AND_CAPABILITY_ARCHITECTURE_V1.md) |
| 0003 | 2026-08-01 | Architecture Blueprint | FMITS Architecture Blueprint V1 | Final | `main` / `d132cea` | [0003_2026-08-01_FMITS_ARCHITECTURE_BLUEPRINT_V1.md](0003_2026-08-01_FMITS_ARCHITECTURE_BLUEPRINT_V1.md) |
| 0002 | 2026-07-31 | Master Map | FMITS Master Map V1 | Final | `main` / `d132cea` | [0002_2026-07-31_FMITS_MASTER_MAP.md](0002_2026-07-31_FMITS_MASTER_MAP.md) |
| 0001 | 2026-07-31 | Audit | Repository Audit | Final | `main` / `d132cea` | [0001_2026-07-31_REPOSITORY_AUDIT.md](0001_2026-07-31_REPOSITORY_AUDIT.md) |

## Document series

Some reports form a sequence and are best read in order. Later reports in a series never modify
earlier ones — they extend them, and record any differences explicitly.

**Architecture series** — the standing architectural reference:

| Read | Report | Answers |
|---|---|---|
| 1st | [0001 — Repository Audit](0001_2026-07-31_REPOSITORY_AUDIT.md) | What is the code? |
| 2nd | [0002 — FMITS Master Map](0002_2026-07-31_FMITS_MASTER_MAP.md) | What is the system? |
| 3rd | [0003 — Architecture Blueprint V1](0003_2026-08-01_FMITS_ARCHITECTURE_BLUEPRINT_V1.md) | How does it work as one architecture? |
| 4th | [0004 — Business & Capability Architecture V1](0004_2026-08-01_FMITS_BUSINESS_AND_CAPABILITY_ARCHITECTURE_V1.md) | Why does it exist, and what must it let the user do? |
| 5th | [0005 — Development Roadmap 2026–2027](0005_2026-08-01_FMITS_DEVELOPMENT_ROADMAP_2026_2027.md) | What do we build next, in what order, and when does each capability become usable? |
| 6th | [0006 — Milestone AF Architecture Gate](0006_2026-08-02_MILESTONE_AF_ARCHITECTURE_GATE.md) | Which single milestone is built next, and is planning finished? |
| 7th | [0007 — Implementation Readiness Check](0007_2026-08-02_IMPLEMENTATION_READINESS_CHECK.md) | Is the repository actually ready to build it? |

For authority when documents disagree, see Appendix B of report 0002.

## Report summaries

One paragraph per report. Migrated here from a root `REPORT.md` that was deleted under explicit
authorization on 2026-08-02, because a generic root report file contradicts the numbering policy
above. This section is the single index; there is no root-level equivalent.

### 0001 — Repository Audit

**Date:** 2026-07-31 · **Type:** Audit · **File:** [`0001_2026-07-31_REPOSITORY_AUDIT.md`](0001_2026-07-31_REPOSITORY_AUDIT.md)

A complete read-only audit of the repository as code: structure, modules, dependency graph, dead and
duplicated files, TODO inventory, documentation drift, git state, and testing architecture. It found
the codebase in unusually good health — 11,128 lines across 17 packages, 3,221 tests passing in 3.84
seconds, 96 % measured line coverage, **zero circular dependencies** across a six-layer graph, zero
runtime dependencies, and a design→implement→review branch discipline followed without exception for
83 commits. Every instance of duplication it found was deliberate and argued for in a docstring; there
are no `FIXME`s, no `XXX`s and no hacks. Its substantive findings were that `fmis.evidence` is a
fully-built, fully-tested module with **no production consumer** while `decision_support` independently
owns the same concept; that the root `README.md` and the "authoritative" architecture document had
fallen roughly two months behind the ADRs; and that the entire safety net is manual — no CI, no type
checker, no coverage tooling, no tags. It closes with the five highest-value architectural
improvements.

### 0002 — FMITS Master Map V1

**Date:** 2026-07-31 · **Type:** Master Map · **File:** [`0002_2026-07-31_FMITS_MASTER_MAP.md`](0002_2026-07-31_FMITS_MASTER_MAP.md)

The definitive answer to *what is FMITS?*, mapping the entire project by domain: mission, philosophy,
principles, product vision, system context, **70 domains across 7 groups**, 20 user-facing products,
the data map, the user journey, and explicit scope boundaries. Every domain carries purpose,
responsibilities, inputs, outputs, dependencies, future expansion, maturity — and a **source tag**
separating approved vision from repository fact from proposal from items with no source at all, so
that nothing in the mission brief was silently promoted into project vision. It records that three
named source documents (`MASTER_PROJECT_CONTEXT`, `MASTER_PROJECT_CONTEXT_TRANSFER`, "Financial OS
Vision") exist in neither the repository nor Google Drive. Its central finding — §15 — is that FMITS
is currently two disconnected systems: a rigorously tested library that analyzes no markets, and a
199-line TradingView prompt that analyzes markets every day and uses none of it. Independent
recommendations are fenced into their own section.

### 0003 — FMITS Architecture Blueprint V1

**Date:** 2026-08-01 · **Type:** Architecture Blueprint · **File:** [`0003_2026-08-01_FMITS_ARCHITECTURE_BLUEPRINT_V1.md`](0003_2026-08-01_FMITS_ARCHITECTURE_BLUEPRINT_V1.md)

The technical architecture reference, reorganizing the same system from a domain axis to a **layer
axis**: twelve layers plus a cross-cutting platform, each with purpose, inputs, outputs, dependencies
and future evolution, with dependencies pointing downward only. It maps the canonical 22-stage
analysis pipeline stage-by-stage onto layers and modules (7 implemented, 1 in progress, 1 planned, 11
future, 2 blocked), defines the four AI boundaries — where deterministic computation ends, where
humans stay in control, where automation begins, and the autonomy line that is not crossed — and
**verifies that the deterministic core is already asset-agnostic**, deriving the *Three Admission
Points* rule that keeps it so. Its headline finding, obtained from the executable import graph, is a
second split *inside* the library: `fmis` is two islands sharing only the kernel with **zero edges
between them**, and the structure island — 5,695 LOC, **51.2 % of the codebase** — has no provider
path and no consumer. The fix is one additive composition root.

### 0004 — FMITS Business & Capability Architecture V1

**Date:** 2026-08-01 · **Type:** Business & Capability Architecture · **File:** [`0004_2026-08-01_FMITS_BUSINESS_AND_CAPABILITY_ARCHITECTURE_V1.md`](0004_2026-08-01_FMITS_BUSINESS_AND_CAPABILITY_ARCHITECTURE_V1.md)

The capability reference, starting from Dovydas's actual financial-market activities rather than from
code: mission architecture, user model, **184 operational capabilities across 19 business
capabilities**, 15 end-to-end workflows, 31 product surfaces, the Global Market Pulse and Buying Power
specifications, 17 business rules with their true enforcement state, a capability dependency map, and
a nine-level value ladder from *tested library only* to *bounded autonomy*. It defines v1 explicitly
(Value Level 3 — a daily market-intelligence workflow) so that "before or after v1" means something,
and it separates every statement into approved intent, verified capability, architectural implication,
independent recommendation, or open question. Its headline is that **6 of 184 capabilities are
implemented**, one of fifteen workflows partially operates — through a prompt — and two of nine of the
project's own success criteria are met. Its practical conclusion, reached from user value and
converging on Report 0003's conclusion from the dependency graph, is the **deterministic fact sheet**:
the smallest slice that makes the unreachable half of the codebase useful, requiring no new engine, no
AI, no persistence and no interface.

### 0005 — FMITS Development Roadmap 2026–2027

**Date:** 2026-08-01 · **Type:** Development Roadmap · **File:** [`0005_2026-08-01_FMITS_DEVELOPMENT_ROADMAP_2026_2027.md`](0005_2026-08-01_FMITS_DEVELOPMENT_ROADMAP_2026_2027.md)

The execution plan, turning the preceding three architecture documents into ordered work: current
position, **thirteen phases** in four movements, a timeline with best/expected/worst estimates
calibrated against the repository's own measured history, a capability unlock map for 23 named
capabilities, a business value curve, a technical-debt strategy naming twelve shortcuts that must never
be taken, a learning roadmap per phase, the critical path, and a 3–5 year vision. It defines v1 as
Value Level 3 and lands it around late 2026. Its most important structural point is that Phases 11–12
are gated by **calendar time, not developer time** — a shadow run takes months regardless of how fast
the code is written. §11 recommends exactly one next milestone: the **deterministic fact sheet**,
reached from user value and converging with Report 0003's dependency-graph conclusion.

### 0006 — Milestone AF Architecture Gate

**Date:** 2026-08-02 · **Type:** Architecture Gate · **File:** [`0006_2026-08-02_MILESTONE_AF_ARCHITECTURE_GATE.md`](0006_2026-08-02_MILESTONE_AF_ARCHITECTURE_GATE.md)

The architecture gate that follows Milestone AF, deciding one thing: the single highest-value
milestone to build next. It rejected the proposed **Workspace MVP** on three grounds — the proposal
was defined nowhere in the project record; every charitable reading fails the rule that presentation
must not precede facts; and the single-timeframe fact sheet AF had just shipped **can actively
mislead**, demonstrated live on BTCUSDT reading `sustained_higher` on 1W, `neutral` on 1D and
`sustained_lower` on 4H — precisely the case `PROJECT_SPECIFICATION_V1.md` §5 says must not be
flattened. It recommends **AG — Multi-Timeframe Fact Sheet** instead, verified live to need no new
engine, and supplies a complete implementation contract in §5. It also reverses the report series'
own prior recommendation of ADR-0020 D1, on the reasoning that AF contained that hazard and
multi-timeframe reuses the same composition root. Ends: *Planning phase complete.*

### 0007 — Implementation Readiness Check

**Date:** 2026-08-02 · **Type:** Readiness Check · **File:** [`0007_2026-08-02_IMPLEMENTATION_READINESS_CHECK.md`](0007_2026-08-02_IMPLEMENTATION_READINESS_CHECK.md)

The readiness check performed immediately before Milestone AG, verifying the repository directly
rather than trusting any document. It found Milestone AF **fully implemented and entirely
unversioned** — 3,305 tests passing in the working tree while `HEAD` and `origin/main` both sat at
the pre-AF commit `d132cea`, with 30 uncommitted paths. It answers six questions (implemented yes;
committed, merged, pushed all no), separates critical blockers from recommendations, and concludes
that the next action was not a milestone but `git commit`. It also corrected a figure that could
mislead: 136 public exports measured **after** AF added nine, coinciding by accident with the
pre-AF baseline recorded elsewhere.

### 0008 — Documentation Consistency Audit

**Date:** 2026-08-04 · **Type:** Audit · **File:** [`0008_2026-08-04_DOCUMENTATION_CONSISTENCY_AUDIT.md`](0008_2026-08-04_DOCUMENTATION_CONSISTENCY_AUDIT.md)

A read-only-first consistency pass across every document in `docs/`, `reports/`, and the
repository root, before resuming feature work on Milestone AO. Found the root `README.md` never
mentioned FMITS or `src/fmis/` at all, describing a pre-FMITS scaffold; `ARCHITECTURE_AND_ROADMAP_V1.md`
— required reading per `docs/README.md`'s onboarding order — frozen at a 147-test baseline with
status tags wrong for several shipped modules and no notice pointing readers at what superseded it;
and two ADRs (0020, 0023) missing forward pointers to the later ADR that closed the hazard they
describe as open. Also found, but left unfixed as explicitly out of scope for a documentation-only
pass: `CURRENT_STATE.md` and `FMITS_PRODUCT_BACKLOG.md` both claim Milestone AN is unpushed when five
further commits have since landed and pushed, and `FMITS_PRODUCT_CHANGELOG.md` §5 still lists AN as
unreleased against its own §3/§4. Zero broken links found across 79 markdown files. Fixed six files
with additive staleness banners and one extended index table; recommended, but did not attempt,
trimming `CURRENT_STATE.md`'s ~900 lines of full historical milestone narrative as the largest
unrealized reading-cost saving in the repository.

## Implementation milestones executed from these reports

Reports produce contracts; milestones deliver product. This table links each executed milestone to
its technical records in `docs/`. The current implementation state always lives in
[`../docs/AI_HANDOFF/CURRENT_STATE.md`](../docs/AI_HANDOFF/CURRENT_STATE.md), refreshed every milestone.

| Milestone | What it delivered | Records |
|---|---|---|
| **AF — First Light** | Connected the deterministic structural chain to real market data: a second composition root in `fmis.pipeline`, a single-timeframe deterministic fact sheet, and the repository's first product surface (`fmits facts SYMBOL`). No new engine. ADR-0020 D1 contained, not fixed | [ADR-0022](../docs/adr/ADR-0022-structural-fact-sheet-composition-root.md) · [design](../docs/design/STRUCTURAL_FACT_SHEET_V1.md) · [review](../docs/reviews/STRUCTURAL_FACT_SHEET_V1_REVIEW.md) |
| **AG — Multi-Timeframe Fact Sheet v1** | A third composition root calling the AF root once per timeframe role (CONTEXT/SETUP/EXECUTION); no cross-timeframe synthesis. `fmits mtf SYMBOL`. Implementation contract was report 0006 §5 | [ADR-0023](../docs/adr/ADR-0023-multi-timeframe-composition.md) · [design](../docs/design/MULTI_TIMEFRAME_FACT_SHEET_V1.md) · [review](../docs/reviews/MULTI_TIMEFRAME_FACT_SHEET_V1_REVIEW.md) |
| **AH — Confirmation-Delay Provenance v1** | Closed ADR-0020 D1: the confirmation delay now travels with the origin that earned it, rather than being a separately-supplied argument that could silently disagree with detection | [ADR-0024](../docs/adr/ADR-0024-confirmation-delay-provenance.md) · [design](../docs/design/CONFIRMATION_DELAY_PROVENANCE_V1.md) · [review](../docs/reviews/CONFIRMATION_DELAY_PROVENANCE_V1_REVIEW.md) |
| **AI — Market Regime Engine v1** | The first interpretation-adjacent layer: `fmis.market_regime`, three separate environment dimensions (structure/volatility/participation), never collapsed into a direction. `fmits regime SYMBOL [--multi]` | [ADR-0025](../docs/adr/ADR-0025-market-regime-engine-v1.md) · [design](../docs/design/MARKET_REGIME_ENGINE_V1.md) · [review](../docs/reviews/MARKET_REGIME_ENGINE_V1_REVIEW.md) |
| **AK — Swing Trading Workspace v1** | Made the third stranded dependency island reachable: `fmis.workspace`, eleven sections (unbuilt ones rendered rather than omitted), deterministic conflict detection that never resolves. `fmits swing SYMBOL`. No ADR — no new boundary | [design](../docs/design/SWING_WORKSPACE_V1.md) · [review](../docs/reviews/SWING_WORKSPACE_V1_REVIEW.md) |
| **AL — Decision Context Engine v1** | `fmis.decision_context` answers one question — does this analysis contain enough trustworthy information to continue — and adds a twelfth workspace section; a policy object carrying no numbers | [ADR-0026](../docs/adr/ADR-0026-decision-context-boundary.md) · [design](../docs/design/DECISION_CONTEXT_V1.md) · [review](../docs/reviews/DECISION_CONTEXT_V1_REVIEW.md) |
| **AN — Deterministic Daily Workflow v1** | The first capability answering about more than one symbol: `fmis.daily`, a per-symbol error-isolated sequential runner, a readiness index that is never a ranking. `fmits daily SYMBOL...`. No ADR — no new boundary | [design](../docs/design/DETERMINISTIC_DAILY_WORKFLOW_V1.md) · [review](../docs/reviews/DETERMINISTIC_DAILY_WORKFLOW_V1_REVIEW.md) |
| **AT — Market Scanner v1** | The first market scanner: `fmis.swing_setup.scan`, a hardcoded twenty-symbol watchlist run through the existing Swing Setup Engine (AR) with per-symbol failure isolation, rendered as one compact table. No new engine, no ranking, no ADR | [design](../docs/design/MARKET_SCANNER_V1.md) · [review](../docs/reviews/MARKET_SCANNER_V1_REVIEW.md) · [report 0009](0009_2026-08-07_MARKET_SCANNER_V1_IMPLEMENTATION.md) |
| **AU — Market Scanner Intelligence Report v1** | `fmis.swing_setup.scan_report`, a second, presentation-only renderer over AT's own scan result: scan summary, market overview, actionable CONFIRMED/CANDIDATE setups with verbatim reasons, and WAIT results grouped by reason. `fmits scan` now defaults to it; `--table` keeps AT's table. No new engine, no ranking, no ADR | [design](../docs/design/MARKET_SCANNER_INTELLIGENCE_REPORT_V1.md) · [review](../docs/reviews/MARKET_SCANNER_INTELLIGENCE_REPORT_V1_REVIEW.md) · [report 0010](0010_2026-08-07_MARKET_SCANNER_INTELLIGENCE_REPORT_V1_IMPLEMENTATION.md) |

## Archive policy

- **Active and recent reports stay directly in `reports/`.**
- Older reports — superseded, or no longer relevant to current work — may be moved into
  [`archive/`](archive/). This is a housekeeping move, not a deletion.
- An archived report **keeps its number, its filename, and its row in the index table above**.
  Only its file link changes, to `archive/NNNN_...md`, and its `Status` becomes `Archived`
  (or `Superseded`, naming the report that replaced it).
- Archiving is never automatic. Move a report only when asked, or when a newer report explicitly
  supersedes it.
- Nothing is ever deleted from `reports/` or `reports/archive/` without explicit authorization.
